# ShopPilot 电商指纹浏览器功能设计

## 1. 产品定位

ShopPilot 是面向电商运营人员的 Windows 桌面浏览器工作台。每个店铺拥有独立的浏览器配置、登录会话、网络配置和标签页状态，用户可以在一个窗口内快速切换店铺并完成日常店铺操作。

第一版定位为“隔离浏览器环境 + 店铺工作台 + 人工确认的操作辅助”。高风险动作（付款、提交订单、发布商品、发送消息、修改收款信息等）必须由用户在浏览器内最终确认。

## 2. 信息架构

- 店铺概览：店铺状态、待办、最近访问、异常提醒。
- 浏览器：店铺列表、浏览器标签页、网页工作区、浏览器环境面板。
- 自动化任务：任务模板、执行记录、暂停/继续、人工确认节点。
- 设置：浏览器配置、代理、数据安全、快捷键、备份与恢复。
- 回收站：已删除店铺和可恢复的数据。

## 3. 按设计图拆分的核心功能

### 3.1 左侧店铺列表

店铺卡片显示：

- 店铺名称、头像颜色、自定义标签。
- 平台名称，例如 拼多多、微信小店、快手小店、抖店（内置四家，另有"其他"供自定义平台）。
- 当前状态：在线、离线、需要登录、代理异常、配置不完整。
- 最近活动时间。
- 未读消息、待处理任务数量。

交互：

- 搜索店铺名称、平台、标签。
- 按平台、状态、最近使用时间筛选。
- 拖拽排序和分组。
- 单击切换当前店铺；双击直接打开其浏览器工作区。
- 右键菜单：打开、重命名、复制配置、导出会话、关闭浏览器、移入回收站。
- 顶部“新建店铺”进入向导。
- 底部回收站显示已删除店铺数量，支持恢复和永久删除。

### 3.2 新建店铺向导

步骤一：填写店铺信息。

- 店铺名称、平台、店铺后台地址、备注和标签。
- 可选填店铺编号、负责人和业务区域。

步骤二：创建浏览器环境。

- 配置名称、操作系统显示、浏览器版本、语言、时区、屏幕尺寸。
- 默认采用稳定配置；修改后需要显示“将影响新会话”的提示。
- 同一店铺始终复用同一套配置，避免每次启动发生不必要变化。

步骤三：配置网络。

- HTTP、HTTPS、SOCKS5 代理。
- 代理用户名和密码使用系统安全存储加密。
- 保存前执行连通性和基础地理位置检查。
- 支持“仅此店铺使用”和“复制到其他店铺”。

步骤四：首次启动。

- 打开店铺后台登录页。
- 用户完成验证码、二次验证和登录。
- 用户点击“已完成登录”后保存会话。
- 登录失败时保留诊断信息，不自动重复提交。

### 3.3 中央浏览器工作区

浏览器区域负责实际网页操作：

- 多标签页、固定标签、拖拽排序、关闭其他标签。
- 后退、前进、刷新、地址栏、收藏、下载和页面搜索。
- 恢复上次打开的标签页。
- 每个标签页显示所属店铺色标，避免误操作。
- 支持常用页面快捷入口：店铺后台、商品、订单、消息、营销、数据分析。
- 页面加载失败显示可操作的诊断卡片：重试、检查代理、重新登录、清理当前站点数据。
- 下载记录按店铺归档，文件名增加店铺前缀，避免混淆。
- 支持截图、复制页面链接和保存书签。

页面安全：

- 跨店铺切换时显示确认提示，防止把操作发到错误店铺。
- 在付款、发布、删除、发送消息等动作前显示人工确认条。
- 禁止后台静默执行需要验证码或二次认证的动作。

### 3.4 右侧“浏览器环境”面板

面板提供当前店铺的环境可视化和操作入口：

- 指纹健康度：配置完整性、参数一致性、最近异常数量。
- 代理位置：国家/地区、IP、延迟、最近检查时间。
- 浏览器配置：浏览器内核、操作系统、语言、分辨率、时区。
- Cookie 状态：已保存数量、最近更新、当前会话状态。
- 启动新浏览器：使用当前店铺独立配置启动新的窗口或标签组。
- 清除浏览数据：仅当前店铺，可选择 Cookie、缓存、LocalStorage、下载记录。
- 编辑浏览器配置：修改前显示影响范围和需要重新登录的项目。
- 锁定环境：禁止任务和快捷键切换配置，适合多人共用电脑。

健康度不是“绕过平台检测”的承诺，只用于提示配置缺失、网络异常、会话过期和参数不一致。

### 3.5 会话和 Cookie 管理

- 每个店铺独立保存 Cookie、LocalStorage、IndexedDB 和缓存。
- 会话数据由 Chromium 在 Windows 用户级加密（DPAPI）保护，应用锁由主密码或 Windows 用户凭据控制；主密码不重新加密会话文件。备份与会话导出包使用用户密码加密，可跨机恢复店铺资料与配置，登录态需重新登录或经加密导出包迁移（详见 DEVELOPMENT_SPEC.md §10.2）。
- 手动导入/导出会话包，导出文件必须加密并设置有效期。
- 显示会话来源、创建时间、最后使用时间和过期状态。
- 支持只清理单个域名，避免清空整个店铺环境。
- 登录失效时通知用户，并提供“重新登录”按钮。
- 导出操作写入审计日志。

### 3.6 代理和网络诊断

- 代理按店铺绑定，支持直连、HTTP、HTTPS、SOCKS5。
- 保存前检测 DNS、HTTP 状态、延迟和 IP 变化。
- 运行中每隔一段时间做轻量健康检查，异常时只提醒，不自动切换到未知代理。
- 支持代理备注、标签、过期时间和批量导入。
- 记录最近 20 次检查结果，便于排查网络问题。

### 3.7 自动化任务

第一版提供可控、可暂停的任务：

- 打开页面并等待加载。
- 批量读取商品标题、库存、价格和订单状态。
- 页面截图和信息采集。
- 表单草稿填充，但提交前必须人工确认。
- 定时提醒登录、检查库存、检查未读消息。
- 任务步骤可插入“等待用户确认”。

任务状态：

- 草稿、排队中、运行中、等待确认、已暂停、成功、失败、已取消。
- 每一步记录时间、页面地址、输入摘要和错误信息。
- 失败后支持从失败步骤继续，不重复执行已完成步骤。
- 验证码、二次认证、付款和发布操作不允许无人值守完成。

### 3.8 店铺概览

概览页以卡片方式展示：

- 店铺总数和各状态数量。
- 今日访问店铺、失败任务、代理异常、需要重新登录的店铺。
- 待处理订单、未读消息、低库存提醒（数据来自页面读取或后续官方接口）。
- 最近活动时间线。
- 点击卡片直接跳转到对应店铺和标签页。

### 3.9 设置和安全

- 应用主题、语言、启动行为、默认标签页和快捷键。
- 主密码、自动锁定时间、Windows Hello/系统凭据解锁。
- 数据目录、备份频率、备份保留数量。
- 日志级别和隐私模式。
- 团队版预留角色：管理员、运营、只读。
- 审计日志支持按店铺、用户、动作、时间筛选和导出。
- 危险操作提供二次确认和可撤销时间窗口。

## 4. 关键数据对象

- Store：店铺基本资料、平台、后台地址、状态、标签。
- BrowserProfile：浏览器配置、语言、时区、分辨率、版本。
- Proxy：代理地址、认证引用、检测结果、有效期。
- Session：加密会话、来源、更新时间、过期状态。
- Tab：店铺、URL、标题、固定状态、最近访问时间。
- Task：任务模板、步骤、当前状态、人工确认节点。
- TaskRun：执行日志、结果、错误、截图。
- AuditLog：用户、店铺、动作、时间、结果、来源。
- Backup：备份文件、校验值、创建时间、恢复状态。

## 5. 第一版（MVP）范围

以下 9 项构成“第一版（MVP）”的完整定义，跨开发里程碑 M1–M4 交付，逐项映射见 DEVELOPMENT_SPEC.md §12.1。

必须完成：

1. 店铺新增、编辑、删除、搜索、筛选和回收站。
2. 每店铺独立浏览器环境和标签页状态。
3. HTTP/HTTPS/SOCKS5 代理配置与连通性检查。
4. Cookie/LocalStorage 等会话数据的加密保存。
5. 浏览器工作区的多标签、恢复、下载归档。
6. 右侧环境面板和基础健康检查。
7. 关键动作人工确认。
8. 审计日志、备份和恢复。
9. Windows 安装包、卸载、自动更新和崩溃日志。

暂不纳入：

- 多平台无人值守发布。
- 自动处理验证码、二次验证或风控挑战。
- 账号共享、云端同步明文会话。
- 大规模爬虫和未授权数据采集。
- 自动付款、自动下单和自动修改收款信息。

## 6. 后续版本

> 里程碑编号（M0–M4）以 DEVELOPMENT_SPEC.md §12 为唯一来源，本节只列方向，不使用 M 编号。

- 任务与采集增强（对应开发里程碑 M3 之后）：任务模板、页面数据采集、库存和订单提醒、代理池管理。
- 团队协作（另立里程碑规划）：角色权限、加密云同步、官方平台 API 连接。
- 企业能力（另立里程碑规划）：统一数据报表、规则引擎、插件系统和企业审计策略。

## 7. 关键验收标准

- 切换两个店铺后，Cookie、缓存、LocalStorage、标签页和代理互不串用。
- 关闭软件并重新打开，店铺列表、浏览器配置和标签页能够恢复。
- 代理不可用时，界面能明确显示原因，且不会静默切换到未知网络。
- 清理数据只影响当前店铺和选中的数据类型。
- 所有高风险提交动作都能在页面内被用户明确确认。
- 任务失败后可以查看步骤级日志，并从失败步骤恢复。
- 备份文件可在另一台 Windows 电脑上通过密码恢复店铺资料、浏览器配置与标签页索引；登录态按加密导出包导入或重新登录迁移，不承诺备份直接携带登录态。
- 安装包在 Windows 10/11 上可安装、启动、升级和卸载。

