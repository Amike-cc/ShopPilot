# ShopPilot 0.4.10 变更说明（M0–M4）

> **构建标签：INTERNAL_BUILD** —— 本包未做代码签名，按 DEVELOPMENT_SPEC §21.5 不得标记为 RELEASE。可用于内部试用与验收，正式对外发布前需补签名证书与验收记录。

## 本次更新（0.4.10）：微信邀约的两个真实问题——筛选点了没生效 / 登录过期看着像"筛选坏了"

按你说的"测试微信小店达人邀约功能"逐项过了一遍，抓到并修掉两个真问题：

### 1. 登录过期不再伪装成"筛选没生效"

微信小店的会话很短（实测几十分钟就掉），过期后广场页只渲染一句「登录超时，请重新登录」——这时筛选根本无从点起，但界面上看就是"点了筛选没反应"。现在「打开达人广场」会先检测登录态，过期就**明确报**：
> 微信小店登录已过期：请在店铺窗口打开 store.weixin.qq.com 扫码重新登录后再试

（真机验证：登录态已过期的店铺上调用，返回的正是这条提示，而不是静默失败。）

### 2. 筛选项"点过了"不等于"筛上了"——改为点击后回读校验 + 失败重试

之前点完就算成功。实测发现某些项（如「美妆护肤」）点一下并不会真的选上（自定义控件/重排导致），于是日志说成功、页面其实没筛。现在每个筛选项点击后**回读它是否真的选中**（最近 label 里的勾选框 checked，或 class 带 on/active/checked/selected），没选上就重试（最多 3 次），最终结果按真实状态回报（`applied` 字段 + 每一项 true/false）。

### 验证

- **仿真页（带 ShadowRoot）回归**：类型「直播带货者」+ 类目「母婴/美妆护肤」+ 其他「有联系方式」→ `applied: true`，三项全 true，页面标签 `已筛选 类型：直播带货者 带货类目：母婴 带货类目：美妆护肤 其他筛选：有联系方式`；
- **真实微信店铺**：登录已过期 → 返回明确的登录过期提示（新行为生效）；
- **商品ID指定**（上一版新增步骤）在仿真表单页复验通过：指定 `10000687986402` → 只勾中该行 → 确认后页面出现该商品。
- **仍待真实页复验**：筛选应用与商品ID添加要在**登录态有效**的真实广场/表单上跑一遍。微信会话很短，需要你在窗口里扫码登录后我立刻复验（登录页与二维码已打开）。

## 上一版（0.4.9）：微信小店达人邀约——广场筛选 + 按商品ID指定邀约商品

按你列的四点把微信小店邀约补齐（数据全部来自真实页面实测）：

### 1-3. 广场筛选：带货者类型 / 带货类目 / 其他筛选

- **面板新增「① 广场筛选」卡片**：带货者类型（全部带货者 / 直播带货者 / 短视频带货者 / 公众号带货者，单选）、带货类目（34 项多选，实测清单：文玩文创、珠宝首饰、家纺、运动户外、母婴、家用电器、数码、鞋靴、家庭清洁/纸品、箱包皮具、个人护理、食品饮料、生鲜、家居日用、家具、酒类、钟表、图书、保健食品、服饰内衣、家装建材、美妆护肤、汽车电动、玩具乐器、教育培训、农资园艺、宠物生活、成人用品、酒旅、餐饮、电脑/办公、手机通讯、厨具、其他）、其他筛选（可开发票 / 有联系方式）。
- **点「打开达人广场」时自动应用**：先把当前标签页导到带货者广场，再按配置把筛选项逐个点上，然后仍由你人工进达人详情页发起邀约（选人这一步平台设计就是人工的）。
- **技术要点（实测踩坑）**：微信页面在 micro-app 的 ShadowRoot 内，普通选择器看不到 → 引擎用深度枚举定位；**微应用不吃合成 click**（合成点击后勾选状态不变）→ 改为先定位坐标、再由主进程发**受信任鼠标事件**（与表单必须走受信任输入同理）。

### 4. 邀约商品：按商品ID指定

- **面板新增「指定邀约商品 ID」**：可填一个或多个（逗号/空格/换行分隔），保存后按 ID 精确添加；留空则仍按原来的"添加 N 个"逻辑。
- **新引擎步骤 `ensureRowsById`**：打开「添加商品」弹窗 → 在弹窗列表里按**行文本包含商品ID**匹配 → 逐个勾选 → 点「确认」→ 复核行数；匹配不到就**如实失败**并提示"请确认商品ID正确且已上架"（不静默跳过、不随便勾别的商品）。
- 门禁提示里会写明本次指定了哪些商品 ID，放行前你能核对。

### 验证情况（如实说明）

- **已真机验证（不依赖登录态）**：在带 ShadowRoot 的仿真页上跑真实应用与真实引擎——
  · 筛选应用：类型「直播带货者」+ 类目「母婴/美妆护肤」+ 其他「有联系方式」全部点中并生效（页面标签显示 `已筛选 类型：直播带货者 带货类目：母婴 带货类目：美妆护肤 其他筛选：有联系方式`）；
  · 商品ID指定：指定 `10000687986402` → 弹窗里**只勾中该行** → 确认后页面出现该商品，步骤结果 `{requested:1, added:1, present:1}`。
- **面板与持久化**：在真实界面上确认「① 广场筛选 / ② 联系方式 / ③ 邀约内容 / ④ 运行」四张卡片、34 个类目与 2 个其他筛选、商品ID 输入框；改动自动落库（`invite.config.微信小店`）。
- **待复验（阻塞项）**：**微信小店登录态已过期**（页面提示"登录超时"，Cookie 为空），所以真实广场/表单上的最终复验还没做。登录页二维码已打开（见 `wx-invite-test/wx-login-qr.png`），扫码恢复登录后我会立刻在真实页面上补跑这两项验证。
- 单测 35 项通过（新增：微信筛选档案、商品ID→ensureRowsById 分支、ensureRowsById schema）。

## 上一版（0.4.8）：主推类目支持二级类目 + 邀约面板 UI 重构

### 1. 主推类目二级选择（面板与执行流程都支持）

- **面板**：主推类目拆成「一级 + 二级」两个联动下拉——一级选好后，二级下拉列出该一级下的子类（默认"不限"= 整个一级）；切换一级时二级自动回"不限"并换选项列表。
- **数据来源**：二级子类是从平台级联弹层**逐个一级实测采回来的真实树**（22 个一级全部到位，存于平台档案 `categoryTree`；"餐饮外卖"平台侧无子类）。个别过长名称平台侧本身有截断（如"摩托车/电动车/自行..."），执行时按包含匹配，不做全等假设。
- **执行**：流程点完一级 chip 后点**所选二级项**（不再固定点"不限"），并在「已筛选」标签行连带校验二级名（如 `主推类目：食品饮料/休闲食品`），没生效就不勾人。
- **实测抓出的平台交互细节**：带下级的二级项（如"休闲食品"）用合成点击只展开下一列、**不选中**——已改为受信任真实鼠标点击（等同用户手点），二级直接生效。
- **配置保存**：二级类目一并持久化（一级/二级联动校验后才恢复）。

### 2. 邀约面板 UI 重构

面板从"一长列控件"重构为三张分区卡片（抖店与微信小店同一结构）：

- **① 选人范围**（抖店）/ **① 联系方式**（微信）：一级/二级类目双下拉、达人等级、本批数量；
- **② 邀约内容**：话术来源（手填/AI）、话术与字数、专属权益（抖店）/ 添加商品数量（微信）；
- **③ 运行**：开始/停止按钮、运行状态、以及"连续开批到额度用完"的说明。

所有原有的 data-test 钩子与配置保存行为不变，只是信息分层更清楚。

### 3. 真实运行验证（真店、面板路径、单批后停止）

- **不发送验证（二级=家清纸品）**：筛选真实生效（「已筛选」同时出现 个护家清 与 家清纸品），该口径下可选只有 8 位 → 任务在发送前**如实失败**并报明"只勾中 8 位（要求 40 位；其中 9 位已邀约/禁用）"——顺带把"池子不足"的如实失败路径也在真机验证了；
- **真实发送验证（一级=食品饮料、二级=休闲食品、LV0–LV3、40 位）**：运行中「已筛选」= `主推类目：食品饮料/休闲食品 达人等级：LV0，LV1，LV2，LV3`，逐个勾满 40 位（页面自认"已选择 40 位达人"），点确认发送后平台提示 **「共发送 40 位达人：33 位发送成功、7 位发送失败」**，随即「停止邀约」成功取消（单批成本）。
- **如实说明**：那 7 位失败是**平台侧逐人结果**（达人设置不接收/细分限制等，页面只给汇总数字），软件当前以"抽屉已关闭=提交被受理"为判据，不拆解逐人成败——已在执行说明里注明。另：本店 个护家清 口径已全部邀尽（0 行可选），这是平台侧状态，不是软件问题。

## 上一版（0.4.7）：邀约配置可保存 + 真实运行验证中修掉三个引擎缺陷

### 1. 达人邀约配置现在会保存（按平台各存一份）

主推类目、达人等级、本批数量、话术、话术来源（手填/AI）、专属权益——在面板里改过之后自动保存（防抖 500ms），重启软件后自动恢复；抖店与微信小店各存各的，互不串。实现上写了两个真 bug 才跑通，都是真机测试抓出来的：

- **IPC 结构化克隆不认 Vue 响应式 Proxy**：保存时快照里的 levels/benefits 是响应式数组，`settings.set` 一直抛 `An object could not be cloned` 且被静默吞掉——现在发送前先转纯对象；
- **`settings.get` 返回 `{key, value}` 包装**：读取时忘了拆包装、直接把包装对象当配置，导致永远读不到存档——现在取 `.value`。

**真机验证**：面板写入特征配置（食品饮料 / LV2+LV4 / 33 位 / 特征话术）→ 存档落库内容一致 → **重启软件后面板自动恢复，逐字段一致**。

### 2. 真实运行验证（按你的要求：改完必须真实操作软件测试）

用真实店铺、从面板点「开始邀约」跑真实流程，顺带抓出并修掉两个引擎缺陷：

- **嵌套步骤超时是 NaN**：loop 的子步骤没有补默认超时（只有顶层步骤会补），真机报 `clickByText 超过 NaNms` 直接失败——现在创建时归一化补默认值，运行时再兜底一次（兼容旧任务）；
- **递归校验把步骤存丢了**：修上一条时第一版递归把子步骤存成了"只有 input 没有 type"，运行报 `未知步骤类型 undefined`——已重构为返回完整步骤结构。
- 另外把面板启动后的提示文案改成与现状一致（抖店已无二次确认）。

### 3. 真实运行结果（如实汇报，含发送量）

- **筛选正确性的铁证**：运行标签页「已筛选」= `主推类目：个护家清/不限  达人等级：LV0，LV1，LV2，LV3`；且流程自带的类目校验步骤（「已筛选」里必须出现所选类目，否则不勾人）**连续 12 轮全部通过**——筛选不是"点过了"，是每轮都被校验过的。
- **循环邀约真实跑通**：每轮约 20 秒完整走完（进广场 → 筛选 → 逐个勾 40 位 → 批量邀约带货 → 额度预检 → 填话术 → 确认发送 → 抽屉关闭校验 → 截图），轮与轮自动衔接。
- **如实说明**：本次是真实发送验证。我原计划"一批后停止"，但测试监听脚本盯错了标签页（后台标签视口为 0、看不到页面提示），等发现时已跑完约 11 批——**本次验证真实发出了约 440 条邀约**，才点「停止邀约」（停止功能本身也验证了：运行立即取消）。该店当日额度远大于 440，"额度用完自动停止"的收尾语义已在仿真站点验证（额度 80/每批 40 → 第 3 轮确认发送禁用 → 运行成功并写明停止原因）；真实店铺上它会在平台禁用「确认发送」的那一刻自动停。
- 单次运行上限 20 批（安全阀）不变；中途随时可点「停止邀约」。

## 上一版（0.4.6）：修复「点开始邀约就失败」

### 现象与定位

你反馈点「开始邀约」后失败。运行记录：**第 1 轮的子步骤 3（点主推类目 chip）** 就报错
`找不到「个护家清」的限定范围（{selector: ".quick-filter-button-enums"}）`，且一个步骤结果都没留下。

根因是 0.4.5 引入 `within`（限定范围）时的一个缺陷：**"范围容器找不到"被写成了立即失败**，
而导航刚完成时达人广场的筛选区往往还没渲染出来（SPA 要挂载一小会儿）——第一下点击撞上空窗期，
整个运行当场失败。就算撞过了这一步，下一步点级联叶子「不限」也会以同样方式失败（级联弹层要点完
chip 才出现）。0.4.5 真机验证时没暴露，是因为当时页面早已加载完成（热页面），没有踩到冷启动的时间窗。

### 修法（两处）

1. **SCOPE_NOT_FOUND 改为在超时内轮询等待**（和"元素没找到"同一待遇）：每 300ms 重找一次范围容器，
   等它随页面渲染出现；整个超时内都没出现才报 `TASK_SELECTOR_CHANGED`，且错误消息会写明
   "超时内未出现限定范围"，与"元素不存在"区分开。
2. **任务运行标签页切到前台**：此前只有微信流程（mirrorTabUrl）会把运行标签页激活，抖店流程（navigate）漏了——
   复用的标签页若不是店铺窗口的当前页，视图视口为 0，可见性判定与点击都会失真。现在运行开始时统一激活。

### 验证

- **直接复现修复点**：仿真站点加了 `?late=1` 模式——筛选区**前 4 秒不渲染**。修复前这种场景必然立即失败；
  修复后任务在 **6.1 秒成功**（等筛选区出现后点中）。
- **真机冷启动回归**（不含发送步骤）：应用重启后直接跑"导航 → 类目 chip → 级联叶子 → 等级 → 搜索 →
  校验已筛选 → 勾 40 位"，全部成功，页面自认「已选择 40 位达人」。
- typecheck + 单测 33 项通过。

## 上一版（0.4.5）：抖店邀约去掉二次确认、修好类目、每批严格 40 位循环到额度用完

### 1. 去掉「再次确认允许」

抖店批量邀约**不再弹人工确认门禁**：点「开始邀约」后一轮做完（勾人 → 批量邀约带货 → 确认发送）自动进入下一轮，中途不再停下来等你点"允许"。
替代它的两道判据：**额度预检**（抽屉「确认发送」不可用就不发）与**发送后校验**（邀约抽屉没关闭 = 平台没接受，如实失败）。微信小店的逐个邀约流程仍保留确认门禁（你没要求改）。

### 2. 修好「主推类目没有正确选择」（真机实测复现 + 修好）

真机实测复现：筛选序列跑完，「已筛选」里**只有** `达人等级`，没有主推类目，列表第一行还是别的类目。定位到两个原因：

- **只点类目 chip 不够**：那个 chip 点下去只是展开**子类级联弹层**（如 个护家清 → 不限 / 个人护理 / 家清纸品），**必须再点一个叶子项**筛选才生效；
- **点错目标的风险**：类目名在达人卡片的类目文案里也有（自身文本同样是"个护家清"），而"不限"在等级下拉里也有同名项——不限定范围就会点错。

修法：类目 chip 的点击限定在「类目快捷选项行」内，叶子项「不限」的点击限定在级联弹层内，点完还**校验「已筛选」标签行里真的出现该类目**（没出现就不往下勾人）。引擎侧为此给 `clickByText`/`waitForText` 加了 `within` 作用域（CSS 选择器或"文本 + 向上爬 N 层"）。

**真机证据（2026-09-13，未发送任何邀约）**：筛完「已筛选」= `主推类目：个护家清/不限  达人等级：LV0，LV1，LV2，LV3`，列表 18 行里 17 行都带「个护家清」；页面自认计数 `已选择 40 位达人`。

### 3. 一批 40 位、循环到额度用完才停

- **严格每批 40 位**：修掉一个溢出缺陷——批量勾选每轮的点击数原来带了 5 位余量，实测**一批勾了 45 位**（用户要求"一次勾 40 位"，多勾就是发错数量），现在严格等于"还差几位"。
- **勾人是一个一个点**：不碰表头的"全选"复选框，只点每行的行复选框；平台对已邀约的行是禁用态，执行时跳过并如实回报跳过数量。
- **向下滚动加载更多**：一屏装不下 40 位，会边滚边勾（虚拟滚动容器，实测每轮滚约一屏）。
- **批次循环**：一轮做完自动开下一轮，直到 **邀约额度用完**（平台「确认发送」变禁用）或**该口径下可选达人不足 40 位**为止——这两种都算**正常收尾**（运行显示成功，步骤结果里写明停止原因），不是失败。单次运行上限 20 批（安全阀，防平台状态异常时无限循环）。

### 4. 引擎侧新增（都走白名单，可复用）

- 新步骤类型 **`loop`**（批次循环）：嵌套步骤**逐个递归过白名单校验**，`stopOn` 命中即干净停止（run 仍成功），其它错误照常失败且消息里带"第几轮/哪个子步骤"；
- `clickByText` / `waitForText` 新增 **`within`** 作用域；
- 文案定位的排序规则改进：自身文本长度相同时**优先可点标签**（容器元素常与内部按钮同样文本，只按长度会先命中容器，点了等于没点——仿真站点验证时踩到）。

### 5. 验证方式（如实说明）

- **仿真站点端到端验证 loop 停止语义**（不碰真实店铺）：额度 80、每批 40 → 实测 `sends=[40,40]`，第 3 轮「确认发送」禁用 → 运行**成功**、步骤结果 `completedRounds=2 / stopReason=TASK_QUOTA_EXCEEDED`；
- **真机验证（不含发送步骤）**：类目生效 + 勾满 40 位，如上；
- **未在真实店铺跑完整的"额度用尽"流程**：那会消耗你当天**全部**邀约额度并向几百位达人发消息，所以留给你自己按需启动。**点「开始邀约」即开始真实发送，请先把类目/等级/话术对好。**
- typecheck + 单测 33 项通过（新增 within / loop 白名单与递归校验用例）。

## 上一版（0.4.4）：任务面板新增二级页签 +「待开发」占位

按你的要求给任务面板加了页签结构，并放一个待开发占位页签：

- **页签栏**：任务面板顶部现在有 `达人邀约`（现状不变，默认打开）与 `待开发`（占位，标签上带一个黄点表示"未开发"）。
- **占位内容**：明确写着**尚未开发**，并说明占位位置与「达人邀约」同级——同样按店铺独立执行，同样沿用这套约束：走任务引擎白名单步骤、发送/提交类动作**前置人工确认门禁**、结果与截图落库留档、失败如实回报。
- **切换不丢状态**：两个面板用 `v-show` 切换（不是 `v-if` 销毁重建）——来回切页签时，正在填的邀约配置、类目/等级/话术都还在。
- **为什么先立结构**：以后加任务类功能只需在页签表里加一条 + 落一个内容面板，不用再动面板布局与信息层级（历史包袱：上一次移除"任务列表"时连二级页签一起去掉了）。
- **实现取舍**：二级页签**不复用**外层 `.panel-tabs` 样式——那一行是标题栏拖拽区、还给原生窗口按钮留了 140px；内容区里的页签若复用它，会抢走拖拽并凭空留白。所以用独立的 `.sub-tabs`。

**实测（真机，重启后新构建）**：页签栏渲染正常；默认页签 = 达人邀约（`invite-panel` 显示、`todo-panel` 隐藏）；点「待开发」正确切换（面板显示互换、高亮跟随）；切回后「开始邀约」按钮与已填配置仍在；typecheck + 单测 28 项通过。

**边界**：本次只加界面结构与占位，**没有新增任何平台能力**，也不影响达人邀约流程。

## 上一版（0.4.3）：抖店达人邀约「一次勾满 40 位」修好并实测确认

你反馈「抖店的达人邀约没有正常运行，要勾选四十个达人进行邀约」。定位到**三个真实缺陷**，全部修在 `clickAll` 这一步，并在真实店铺实测到 **页面自己确认「已选择 40 位达人」**：

1. **筛选后表格还没排完版就开点** → 原来的批量勾选是"单发"：筛选/搜索后行还在渲染，行高为 0 会被判成"不可见"全部跳过（实测 15 项里 14 项被跳过、只勾中 1 项）。现在每轮先轮询等到"出现可点的新行"再动手，超时内重试。
2. **列表是虚拟滚动，一屏装不下 40 行** → 广场表格在固定容器里滚动（实测整表约 60 行、可选约 46 位），一屏只渲染约 19 行，而且**容器带 `scroll-behavior: smooth`**：赋完 `scrollTop` 立刻回读还是旧值，于是"滚没滚"永远判成没滚、续选一轮就退出（这就是"勾不满"的直接原因）。现在强制 `auto` 后滚动、等位置落定再判定，并向下续选直到够数或池子扫空。
3. **勾选数与页面自认不一致** → 虚拟列表会回收行节点，有的行"表格已选中、复选框渲染还没同步"，照 DOM 的 `checked` 判断就会把**已选行点反选**（实测点了 40 次、页面只认 35 位）。现在按行 `data-row-key` 去重（同一行只碰一次）、每点一次回读页面自己的「已选择 N 位达人」，没涨就再点一次纠正；**并把页面计数当权威值**——勾不满就在发送前如实失败（新错误码 `TASK_SELECTION_SHORTFALL`），不再按错的位数往下走。

**实测记录（2026-09-13 22:53，真实店铺 · 类目 个护家清 · 等级 LV0–LV3 · 最多 40 位）**：

- 任务步骤 `clickAll` 结果：`clicked 42 · pageSelected 40 · selected 40 / requested 40 · rounds 7 · scrolled 6 · skippedDisabled 5 · retried 1`；
- 页面上「已选择 **40** 位达人」独立复核一致，随后正常走到**发送前人工确认门禁**（本次**未发送**：40 条真实邀约不可撤回，留给你自己点确认）。

**如实说明的边界**：可勾选的是"未邀约过"的达人，平台对已邀约行禁用复选框——筛选后可选池子不足 40 位时，任务会**在发送前明确失败**并告诉你实际可选数量，而不是少勾了就发。发送链路本身在 0.4.1 已真实验证（发出 1 位并复核到「发过消息」标记）。

### 真实发送：40 位端到端发出并获平台确认（2026-09-13 23:13，真实店铺）

按你的要求跑完了整条链路：**达人广场勾选 40 位 → 批量邀约带货 → 弹出批量沟通 → 确认发送**。平台自己的提示是最终判据：

- **平台侧确认**：发送后页面弹出「**40 位达人邀约带货发送成功，待达人回复 查看IM**」（截图留档：`<userData>/stores/<storeId>/artifacts/run_85e1f3928fb56a68cd4b62fe_step18.png`）。
- **流程判据**：`clickAll` 勾选到页面自认 **40 位**（`selected 40/40`）→ 批量沟通抽屉打开 → 「确认发送」可用 → 点击后**抽屉自动关闭**（`waitForGone` 通过，平台已受理）→ 自动截图留档。
- **逐人复核**：抽查 8 位达人昵称，其中 4 位在广场已显示「**发过消息 / 继续沟通**」且复选框 **disabled**（另 4 位正是批次开头"点了没选上"的行，见下方说明）。
- **列表变化**：同一筛选（个护家清 / LV0–LV3）下，可邀约列表从 40 位已选 + 3 位已邀约，降到**只剩 1 位可选**——平台已把这批人移出可邀约池。

**如实说明的两点**：① 本次 `clicked 46 / selected 40`——批次开头有 6 次点击没粘住（虚拟列表回收行节点），最终**以页面计数 40 为准**，发出去的就是选中的那 40 位；② 运行记录里目前只落库 8 个抽样昵称，**完整 40 位名单尚未留档**（已列入下一步：把全部受邀行 key/昵称写入运行记录，便于事后逐人核对）。本次真实发送消耗 40 个邀约额度。

## 上一版（0.4.2）：移除「任务列表」——任务只作为功能的执行引擎

按你的要求，任务不再需要一个列表界面。**任务引擎原样保留**（它是达人邀约与数据中心采集的执行底座），移除的是面向用户的「任务列表」UI：

- **删除**：任务面板的二级页签（任务列表 / 达人邀约）、任务列表本身（任务卡片、步骤明细、暂停/恢复/取消/删除按钮、定时标记）、以及「+ 新建任务」入口与整个任务编辑对话框（含步骤模板与步骤编辑器）。
- **现在**：任务面板只有「**达人邀约**」一个内容，进入即见，不再需要先选页签。
- **结果与失败信息不会因此消失**（这是移除列表前必须补上的出口）：
  - 邀约：面板自带「**邀约记录**」（状态 / 逐步结果 / 门禁日志 / 发送截图，近 5 次）——不变；
  - 采集：数据中心按店铺展示数字与「较上次采集」；**新增采集结果逐店汇报**——采集结束后弹出一条汇总，例如 `采集结果 —— 快手小店 ✓｜微信小店 ✗ TASK_SELECTOR_CHANGED：页面上找不到文案为「近7天」的可点击元素`，失败原因不再被埋进看不见的运行记录里。
- **实测验证**（重启后）：任务面板 `二级页签=空、任务列表卡片=0、新建任务按钮=0`，达人邀约面板直接呈现，邀约记录仍有 5 条历史。
- 如实说明的取舍：任务列表里的"取消排队/暂停运行"入口随之消失——采集任务若因店铺浏览器未打开而排队，会一直等到你打开该店铺（引擎设计本就不静默拉起）；已结束的运行记录仍留在库里（邀约的在邀约记录、采集的体现在数据中心数字上）。

## 上一版（0.4.1）：抖店邀约「真实发送」已验证；新增发送后结果校验

### 真实发送：端到端验证通过（2026-09-13 22:12，真实店铺）

此前所有验证都停在确认门禁（未发送）。本次按你要求**真实发送了一位**：

- 发送对象：**野王知遇（涨粉中）**（食品饮料/美妆，有联系方式）；话术 45 字；平台推荐商品自动带 3 个；额度预检通过。
- **发送前的证据**：该行复选框**可勾选**（批量勾选步骤报 `skippedDisabled: 0`，成功勾中 1 位）。
- **发送后的证据**（刷新列表、按昵称搜索复核同一行）：出现「**发过消息**」标记、操作变为「继续沟通」，且该行**复选框已禁用** (`disabled=true`)。
- 平台对"已邀约过"的达人会禁用该行复选框、不允许重复邀约——这正是平台侧已记录本次邀约的行为。抽屉在发送后**自动关闭**。

### 新增：发送后结果校验（`waitForGone` 步骤）

发送类任务过去以"点了确认发送"作为成功判据，**无法区分"发出去了"与"点了个没反应的按钮"**。本次新增 `waitForGone`（等元素消失或不可见）：

- 抖店邀约流程在点击「确认发送」后，会等待**邀约抽屉关闭**——抽屉还在就说明平台没接受这次提交，任务**如实失败**，绝不把"点过了"当成"已经发出去了"。
- 同时新增一步**截图留档**（发送后的页面状态事后可查）。
- 正反两面都验证过：仿真页上"打开弹窗→点确认关闭"→ 校验通过（任务 succeeded）；"打开弹窗不关"→ 校验超时**如实失败**（`TASK_TIMEOUT`）。

### 如实提醒

- 本次真实发送**消耗了 1 个邀约额度**，并向该达人发出了真实消息——请在抖音/巨量百应的消息或邀约记录里核对。
- 平台侧多次提示「**您尚未填写平台公开联系方式**」（可能影响达人合作转化）：本应用按设计不代填、不保存你的手机号/微信号，建议你到平台侧补填一次。
- 抖店其余已知边界不变：额度只判"能否发送"（页面不展示剩余数量）、AI 生成话术尚未在真实店铺端到端跑过、频繁自动操作可能触发验证码。

## 上一版（0.4.0）：拼多多——实测确认不可自动读取，改为「手动录入」

### 实测结论：拼多多的数字在 DOM 与接口里都是混淆码位

本次把拼多多挖到底了，三条证据：

1. 页面会先请求 `api/phantom/web/en/ft` 取回 **`web_spider_rule`（带种子的规则串）+ 专用字体 `ttf_url`**，再用该字体渲染数字；
2. **DOM 里不是数字字符**：指标值元素是 `class="value … __spider_font"`、`font-family: spider-font`，读到的是私有区码位（实测只有"较上期 X%"这类百分比可读）；
3. **接口 JSON 里也是混淆的**：数据中心自己的响应里 `"payOrdrAmt":"…"`、`"prevYearTotalSalesAmount":"…"` 的值同样不是数字字符。

也就是说，"读接口"和"解字体"都等于**持续对抗平台明示的反自动化措施**（规则串按会话下发种子、字体带内容哈希）。**本应用不做这种绕过**——这是产品红线，不是技术做不到。

### 因此新增：经营指标「手动录入」

- 数据中心「经营指标」下方新增一行**手动录入**：选店铺 → 选指标（销量/订单/销售额/退款金额/退款订单数）→ 填值 → 录入。
- 录入值同样落 `store_snapshots`（`source_run_id` 留空 = 手动来源），界面**明确标注「手动」徽标**，并如实计入合计——**绝不与自动采集的数字混淆**。
- 录入接口做了白名单校验：指标名仅允许 `biz.*`、值须为 0～1e12 的数字，并写审计。
- **实测**：给「慕么美（拼多多）」录入销售额 `1234.56` → 表格显示 `¥1,234.56 手动`，合计随之更新为 `¥1,311.49 · 已采集 4 家`（快手 46.97 + 拼多多 1234.56 + 微信 0 + 抖店 29.96）。

### 数据中心现在的四平台状态（全部实测）

| 平台 | 采集方式 | 指标 |
|---|---|---|
| 快手小店 | 自动 | 销量 3 / 订单 3 / 销售额 ¥46.97 / 退款金额 ¥12.99 / 退款订单数 1 |
| 抖店 | 自动 | 成交金额 ¥29.96（今日口径） |
| 微信小店 | 自动 | 订单 0 / 销售额 ¥0 / 退款金额 ¥0 |
| 拼多多 | **手动录入**（平台反抓取） | 由用户自行录入 |

## 上一版（0.3.2）：抖店接入经营指标（成交金额）；采集跑完自动刷新数据中心

- **抖店接入**（真实登录态实测）：后台首页 `fxg.jinritemai.com/ffa/mshop/homepage/index` 卡片上的「**成交金额**」已可采集（实测 ¥29.96，口径：今日）。**只登记能可靠读到的一项**：该账号罗盘未开通，且首页的"销量/退款"等标签与广告文案同名（实测「销量」读到的是"销量高"这类推广文案），因此不登记、不猜。
- **指标取不到数字时如实失败**：`readLabelValue` 作为指标落库前会校验"确实取到了数字"——取不到就报 `TASK_SELECTOR_CHANGED` 并**不落库**，杜绝把 "(退款时间)" 这类文字当指标值写进数据中心。
- **采集跑完自动刷新数据中心**：此前采集流程只在自身等待窗口内刷新；如果某次运行因**排队等店铺浏览器打开**而在窗口之后才跑完，表格会停在旧数字上（本次实测遇到）。现在任何运行结束（finished/failed）都会刷新已打开的数据中心。
- 实测三平台采集结果：**快手** 3/3/¥46.97/¥12.99/1；**微信小店** 订单0/销售额¥0/退款金额¥0；**抖店** 成交金额 ¥29.96。「较上次采集」列同步生效（数据未变时显示"持平"）。

## 上一版（0.3.1）：微信小店经营指标跑通；数据中心支持合计与增减

### 微信小店：采集跑通（实测）

真实登录态后台实测通过，数据中心的「微信小店测试」一行已出数字：**订单 0 ｜ 销售额 ¥0 ｜ 退款金额 ¥0**（采集时间 21:41:26；该店当期确实无成交，0 是真实值）。销量与退款订单数该店数据页无内容 → 如实显示「—」。
如实提醒：微信登录是**会话型 Cookie，应用一重启就掉**（本次验证后又重启过一次，需重新扫码才能刷新数字）——采集本身已验证可用。

### 数据中心完善

- **合计行**：所有已采集店铺的 销量/订单/销售额/退款金额/退款订单数 求和，并如实标注「已采集 N 家」「各平台自报口径」——只对**已采到的数值**求和，缺失项不按 0 硬凑。
- **较上次采集的增减**：每个指标旁显示 ▲/▼/持平 与增减量（后端改为按「店铺 × 指标」返回最近两条快照）。第一次采集没有对比对象时不显示。
- **数字显示规范**：金额统一 `¥` + 千分位（最多两位小数），计数类取整；**未采集显示「—」**（不再显示"未采集"字样，表格更清爽，口径说明仍在表下）。
- **金额格式化**、合计与增减都在真实数据上验证：快手 `3 / 3 / ¥46.97 / ¥12.99 / 1`，合计行「已采集 2 家」。

### 顺带修复

- **`BROWSER_CLOSED` 成为独立错误码**：此前"店铺浏览器或任务标签页被关闭"会落进 `INTERNAL_ERROR`，看不出真实原因（实测快手一次采集就因此失败）。现在如实归类，界面直接显示"店铺浏览器或任务标签页已被关闭（运行中止）"。
- 任务运行标签页按店铺复用（0.2.4 引入）在本次验证中生效：反复采集不再新增标签页；本次清理了验证过程里堆出的 5 个重复微信标签页。

## 上一版（0.3.0）：AI 配置支持任意模型与中转站；微信小店经营指标接入

### AI 配置：从"只能填完整补全地址"到"任意地址都能用"

此前必须填到 `/chat/completions` 结尾——而**中转站/自建网关给的通常只是"基础地址"**（如 `https://api.xxx.com` 或 `https://api.xxx.com/v1`），填进去 /models 推不出来、请求也打到错路径。

- **基础地址与完整地址都能填**，按可预期的规则自动规范化（不是猜可用性；拼出来的地址照样要经「测试连接」验证）：
  `https://host` → `https://host/v1/chat/completions`；`https://host/v1`（以及 `/v4`、`/compatible-mode/v1` 等版本段）→ 补 `/chat/completions`；已以 `/chat/completions`、`/completions` 结尾的原样保留。
- **界面显示"实际请求地址"与"模型列表地址"**：填完立刻能看到到底会请求哪个 URL，中转站填错路径一眼可见。
- **服务商预设 8 项**：DeepSeek / OpenAI / 通义千问（阿里云百炼）/ Kimi / 智谱 GLM / 硅基流动 / OpenRouter / **中转站·自定义**。预设只是便捷预填，地址与模型名都可任意改；手改地址会自动切到"自定义"。
- 规范化与 /models 推导都有单测覆盖（含"自定义前缀"与"贪婪匹配"回归），全套 28 项单测通过。实测界面：输入 `https://api.my-relay.cn` → 实际请求 `https://api.my-relay.cn/v1/chat/completions`、模型列表 `https://api.my-relay.cn/v1/models`。

### 经营指标：微信小店接入（数据中心）

- **微信小店档案**（2026-09-13 在真实登录态后台实测）：后台首页「经营数据」区可读 **成交金额 / 成交订单数 / 成交退款金额** 三项；整页在 `<micro-app shadowdom>` 的 ShadowRoot 内，因此锚点读取与周期点击都走 `deep` 穿透。周期控件文案实测为「**近7天**」（与快手的「近7日」不同——实测为准，不套用）。
- **销量与退款订单数未采集**：本店「店铺数据 → 交易统计」页渲染为空（无数据/权限），如实留空、不猜别的口径。
- **登录态是会话型**：应用重启即掉——实测未登录时采集任务**如实失败**（`TASK_SELECTOR_CHANGED`，找不到周期控件），不会填假数字。
- 快手小店实测复跑通过：销量 3 / 订单 3 / 销售额 ¥46.97 / 退款金额 ¥12.99 / 退款订单数 1（采集时间 21:32:30，口径近7日）。

### 拼多多：实测结论（不登记）

拼多多后台一页就有全部五项（含"商品成交件数"=销量），但**数值是刻意防抓取的**：页面会先请求 `api/phantom/web/en/ft` 取回 `web_spider_rule` + 专用字体 `ttf_url`，再用该字体渲染数字——DOM 文本是乱码、只有百分比可读。接口 JSON 里虽是明文数字，但那是平台明示的反自动化措施（绕过它属于另一回事，且有用风控风险），**本版不实现、不登记**。若确需读它，可行方向是"截图 + 识别（OCR/视觉模型）"——需要你确认后再做。

### 抖店（1111）：无数据页权限

罗盘显示"功能建设中"、订单/售后页不落到数据模块，仅订单页文案「共722条订单」可读——不足以支撑五项指标，故未登记。

## 上一版（0.2.9）：数据中心经营指标——快手小店五项跑通

**本次完成**：
- **快手小店五项指标全部接通**（真实店铺端到端验证）：销量 **3**、订单 **3**、销售额 **¥46.97**、退款金额 **¥12.99**、退款订单数 **1**（采集时间 21:15:33）。数据来自生意通「商品总览」页（`syt.kwaixiaodian.com/zones/goodsManagement/goods_overview`）——一页覆盖五项。
- **口径显式固定并在界面标注**：实测该页默认周期是"昨日"（多为 0），而交易页默认近 7 日有值——数字没有口径比没有数字更糟。因此采集流程先点一次「近7日」统一定口径，界面下方如实显示每个平台的口径说明（如"销量取成交件数、退款金额取退款金额(退款日)"）。
- **引擎新增两个原语**：
  - `waitMs`（显式等待）：点完周期后页面是**原地刷新数值**而不是新增元素，任何"等元素出现"的判据都会立刻命中旧值（实测踩过：读到的是切换前的 0）→ 只能显式等页面刷完再读。
  - （上一版已加的 `readLabelValue` 按标签文案读值，规避了平台类名的构建哈希。）
- **采集完成后自动刷新**：采集要跑十几秒到一分钟，弹窗会自动等运行结束再刷新数字（不再停在旧值上）。
- **其余三个平台的如实结论**：
  - **拼多多**：后台数据中心一页就有全部五个指标（成交金额/成交订单数/**商品成交件数**/成功退款金额/成功退款订单数），**但数值做了反抓取字体混淆**（`class="__spider_font"`、`font-family: spider-font`，DOM 文本是乱码、只有百分比可读）。**不登记、不填乱码**——这是"宁可没有、不可造假"的红线。要读它只能走图形识别（截图+OCR/视觉模型），属于另一项能力，需要你确认再做。
  - **微信小店**：后台首页确有「成交金额/成交订单数/成交退款金额」等经营数据，但**应用内需要重新扫码登录**（会话型 Cookie，重启即掉）——窗口里就是二维码，扫码后我立即实测锚点接入。
  - **抖店（1111）**：该账号**没有后台数据权限**（罗盘显示"功能建设中"、订单/售后列表页不落地到数据模块），只有订单页文案"共722条订单"可读——不足以支撑五项指标，故未登记。

## 上一版（0.2.8）：数据中心新增「经营指标」——销量/订单/销售额/退款金额/退款订单数

**数据从哪来（先说清）**：本应用没有平台官方 API，这几个数字**只能从各平台后台页面读取**，走既有的「任务读取 → 指标快照」机制；数据中心再按「店铺 × 指标」取最新一条展示。

- **数据中心新增「经营指标」区块**：一张跨店铺的表（店铺 / 平台 / 销量 / 订单 / 销售额 / 退款金额 / 退款订单数 / 采集时间），金额类自动加 `¥`，没采到的如实显示「未采集」；「采集经营数据」按钮为**每个已实测平台的店铺**各创建一个只读采集任务并立即运行（店铺浏览器未打开则排队等待，不静默拉起，§4.4）。
- **引擎新增读取步骤 `readLabelValue`（按标签文案读值）**：实测发现各平台指标卡片的类名普遍带**构建哈希**（快手 `kpro-data`、微信 `weui` 皆如此），写死选择器会随版本失效；而标签文案（「成交金额」「退款订单数（支付日）」）是平台对外的稳定表达。该步骤以文案为锚：找到标签元素后向上取"只多出这一小段文本"的最近祖先，那段文本即值；值里常见的「较上期比上周 X%」对比噪音只取开头数字（含万/亿换算），爬到容器层级（文本过长）则**如实报 `TASK_SELECTOR_CHANGED`**，绝不从整页噪音里猜数字。
- **首个实测平台：快手小店**（2026-09-13 在真实登录态店铺实测）。一次导航覆盖四个指标：生意通 → 交易（`syt.kwaixiaodian.com/zones/tradeManagement/dealAnalysis`）→ 成交金额 / 成交订单数 / 退款金额（支付日）/ 退款订单数（支付日）。**实测端到端结果**：采集任务 succeeded，读出 `¥46.97 / 3 / ¥12.99 / 1`，数据中心如实汇总（其余店铺显示「未采集」）。
  - **销量**：该页无对应标签（生意通交易页只有成交金额/成交订单数/成交人数等），**留空不猜**——需要按平台逐个实测补齐。
- **未实测的平台如实拒绝**：拼多多 / 微信小店 / 抖店当前显示「未采集」，采集按钮会说明"尚未实测锚点"。其中：微信小店后台首页确有「成交金额/成交订单数/成交退款金额」等经营数据，**需要登录后实测锚点**（会话型 Cookie，重启即需重新扫码）；抖店该店铺无后台数据权限（罗盘"功能建设中"、订单/售后页不落地）；拼多多尚未实测。

## 上一版（0.2.7）：数据中心入口移到左下角

- **入口位置按使用习惯调整**：从右栏底部（右下角）**移到左栏底部（左下角）**，仍保持「**单独占一行**」——在 `回收站 / 截图 / 设置` 那一行上方，整行显示「📊 数据中心 · 全部店铺」。
- **左栏收起为窄轨时**：`📊` 图标**钉在窄轨最底部**（其余图标仍在顶部），左下角位置不变、始终可达。
- **右栏不再重复出现**该入口（原右栏底部行与窄轨图标已移除），避免同一功能两处入口带来的困惑。
- 之所以更合适：数据中心的入口与"当前显示哪家店铺/右栏是否收起"无关，放在始终可见的左栏底部更稳。
- **验证**：展开态实测入口距左 12px、整行宽 279px（左栏 320px 内），位于左栏底部；收起态实测 📊 在窄轨 `top=862 / 底部 900`（其余按钮在 16/52/88/124），即钉在底部。typecheck 与 21 项单测全绿。

## 上一版（0.2.6）：为「静默退出」补齐可追溯性

**现场**：2026-09-13 20:33 应用进程**无声消失**——日志最后一行是启动记录（无任何报错）、Windows 事件日志无记录、无崩溃转储。事后完全无法判断是「窗口被关 → 应用退出」「主动退出」还是「被外部结束」。0.2.4 已覆盖渲染进程崩溃与异常风暴，但**主进程静默退出**当时没有留痕。

**修复**（只加留痕与转储，不改变任何行为）：
- **退出路径逐点留痕**：主窗口 `close`、`window-all-closed`（Windows 上全部窗口关闭即退出应用——静默退出最常见的原因）、`before-quit`、`will-quit`、`quit`、`process exit` 全部写日志。下次再发生，日志会直接说明走的是哪条路径。
- **本地崩溃转储**：启用 `crashReporter`（`uploadToServer: false`，只写本机 `crashDumps`、绝不上传，与 §21.5「上传默认关闭」一致）；原生崩溃会留下 `.dmp`，启动日志记录转储目录。
- `before-quit` 里原先只有 `console.log` 的收尾动作改为同时写日志（控制台不可见时等于没记录）。

## 上一版（0.2.5）：数据中心（汇总所有店铺的数据）

- **入口**：右栏（窗口右下角）底部新增「📊 数据中心 · 全部店铺」**独立一行**，钉在面板底部；右栏收起为窄轨时保留 `📊` 图标入口。数据中心与具体店铺无关，任何店铺视图下都可打开。
- **内容**（全部读自本机既有数据，不做估算补数，没有就显示空状态并说明如何产生）：
  - **概览卡**：店铺 / 在线 / 已归档 / 回收站 / 平台数 / 指标快照数 / 任务数；
  - **按平台**：各平台店铺数与在线数；
  - **指标快照**：任务 `readText` / `readTable` 步骤按「指标名」落库的快照，**每店每指标取最新一条**（店铺 · 指标 · 值 · 采集时间）；
  - **邀约运行**：达人邀约独立功能跨店铺的状态分布 + 最近 10 次（店铺 · 邀约 · 状态 · 时间 · 错误码）；
  - **任务运行（近 7 天）**：状态分布 + 最近失败/取消明细（含错误码）。
- **实现**：新增只读 IPC `overview:datacenter`（`stores` / `store_snapshots` / `task_runs` 三张既有表的聚合查询），弹窗沿用既有遮挡处理（打开时摘除原生视图挂载，避免弹窗被店铺页面盖住）。
- **验证口径**：21 项单测 + typecheck 全绿；独立临时实例上端到端验证（造数据 → 入口 → 弹窗）：跑一个 `readText` 指标任务产生快照、跑一条邀约运行，数据中心如实汇总出 `invite.quota = 200`、平台分布、邀约/任务状态分布与明细，`DATACENTER-VERIFY: PASS`。

## 上一版（0.2.4）：卡死 / 闪退 / 崩溃专项排查与修复

**排查方法**：先把近三天应用日志做模式归类（异常/渲染进程消失/未处理拒绝），再对当前进程内存与标签页数量取证，最后用独立临时实例做"故障注入"复现验证。

### 1. 卡死：日志写入的异常递归风暴（真实故障，已复现并根治）

日志证据：`2026-09-13 18:11` 的 **8 秒内 11,787 条 `uncaughtException: EPIPE: broken pipe`**（约 1,470 次/秒，CPU 被异常处理打满，表现就是"卡死"）。

根因链：应用从已关闭的终端/shell 启动 → stdout/stderr 管道断开 → `logMain` 末尾**未加保护的 `console.error` 抛 EPIPE** → 全局 `uncaughtException` 处理器又调用 `logMain` 记录它 → 再次 console.error → 再次抛错 → **无限递归**。

修复：
- `logMain` 的控制台镜像写入加 try/catch，并在首次失败后**永久熔断镜像**（文件日志不受影响）；
- 同时监听 stdout/stderr 的 `error` 事件，把异步 EPIPE 在源头吞掉；
- `uncaughtException` / `unhandledRejection` 处理器改为**绝不抛错**的写入路径，并加**限流**（同类异常 10 秒内最多记 20 条 + 1 条汇总），避免日志自身把磁盘/CPU 打满。

验证（故障注入）：从脚本以管道拉起应用 → 销毁管道 → 注入 60 次渲染层错误 → **`uncaughtException=0`、应用存活**（旧代码同触发 1.1 万条）。

### 2. 闪退/白屏：渲染进程崩溃无留痕、无恢复

- **店铺标签页崩溃**：此前只弹一个 toast，日志里查不到（此前诊断只能靠猜）。现在写入 `store tab renderer gone store=/tab=/reason=/url=`，并**有界自动重载**（同一标签页 5 分钟内最多 1 次，防"崩溃-重载"死循环）。
- **主窗口渲染进程崩溃**（用户看到的"闪退/白屏"）：此前只记一行日志。现在**自动重载最多 3 次/5 分钟**；超过则停手并弹窗如实告知（附导出诊断指引），不做无限重载。
- **无响应（"卡死"）留痕**：主窗口与店铺标签页都监听 `unresponsive`/`responsive` 并写日志——这类问题事后只能靠日志定位。

验证（故障注入）：对店铺标签页与主窗口分别发 CDP `Page.crash` → 两者都**留痕 + 自动恢复且恢复后可用**。

### 3. 内存持续增长：任务运行标签页按店铺复用

取证：实测运行数小时后有 **10 个渲染进程、合计约 1.5GB**。原因：任务引擎**每次运行都新建一个标签页且从不回收**（验收过程中一天内堆了 7 个达人广场标签页）。

修复：运行标签页**按店铺复用**（浏览器关闭导致 id 失效时自动重建）。复用不改变步骤语义——抖店流程首步是 `navigate`、微信流程首步是 `mirrorTabUrl`，都会把页面重新加载到目标地址。

验证：同一店铺连续跑两次任务，标签页数量 `2 → 3 → 3`（不再增长）。

### 4. 未发现的问题（如实说明）

- `unhandledRejection` 三天内 0 条；主进程无未捕获异常（除上述 EPIPE 递归）。
- 9-11 的 11 次主窗口渲染崩溃与 1 条 `ReferenceError: Cannot access 'ctx' before initialization` 均属早期开发日，其后两天未复现；现在这类事件都会留痕，若再现可直接定位。
- 崩溃报告上传仍默认关闭（§21.5），本轮只做本地留痕与自恢复。

### 验证口径

21 项单测 + typecheck 全绿；上述三项均由 `wx-invite-test/verify-stability.js` 在独立临时实例上故障注入复跑（`STABILITY-VERIFY: PASS`），不影响正在运行的主实例与微信登录有效期探测。

## 上一版（0.2.3）

- **达人邀约成为完全独立的功能，不再进「任务列表」**。此前点「开始邀约」会在任务列表里生成一条任务卡片，与读取型任务混在一起；按"功能独立、对应每个店铺"的模型，现在邀约运行不进入任务列表。
  - 邀约面板自带「**邀约记录**」区（仅本店铺、近 5 次）：时间、状态、可展开看每一步的结果与门禁日志、进行中可就地停止、可删除记录——发送截图等工件也在这里查，审计链不断。
  - **人工确认门禁不受影响**：确认条本就渲染在右栏顶部、任何面板/任何页签都可见（此前专门修过"门禁躲在任务面板里导致看起来卡死"的问题），隐藏任务不会让确认按钮消失。
  - 文案同步：面板说明明确「独立功能，不进任务列表」，任务列表空状态指向「达人邀约」页签。
- **验证口径**：21 项单测 + typecheck 全绿；临时实例（独立 userData，不影响正在运行的主实例与登录有效期探测）实测：邀约运行出现在「邀约记录」（含历史 5 次明细）且不在任务列表、门禁确认条可见、拒绝后运行取消并如实记录——全部通过。

## 上一版（0.2.2）

- **修复：抖店「开始邀约」点不动/必失败**。复现实录：点「开始邀约」后任务能创建并启动，但在第 3 步（点主推类目）就以 `TASK_SELECTOR_CHANGED` 失败——达人广场是 SPA，筛选区在导航完成后才渲染，而 `clickByText` 此前是**单发点击**（找到才点、找不到立刻失败），必然撞空。已改为**轮询式**：在步骤超时内每 300ms 重找一次，出现即点，禁用态立即如实失败。
  - 另如实提醒：页面上出现过 `rmc-nocaptcha` 风控验证 iframe——频繁自动操作可能触发抖店风控；出现验证码时请在页面上手动完成后重试。
- **邀约面板新增「开始邀约 / 停止邀约」状态**：邀约任务在途（排队/运行/等确认/暂停）时，面板显示「邀约进行中 · 状态」+「停止邀约」按钮（一键取消运行），且不再重复出现「开始邀约」；运行结束后自动回到可开始状态。开始后停留在邀约面板（不再跳去任务列表），任务明细仍在「任务列表」页签可查。
- **邀约前先检测额度（两平台）**：新增两种读型预检步骤（无副作用、可重试）：
  - `requireQuota`：在可见元素自有文本里找额度标记并提取数字，低于下限即以 `TASK_QUOTA_EXCEEDED` 如实失败。**微信小店**：镜像邀约页后立即读取「今日剩余N次邀请机会」，为 0 不进入填表/发送；额度数字同时写入店铺指标快照（`invite.quota`）供事后查证。
  - `requireEnabled`：**抖店不在页面展示剩余额度数字**（实测列表页与邀约抽屉均无），额度用尽/平台限制的表现是抽屉「确认发送」按钮变禁用——因此在打开邀约抽屉后、填话术与人工确认之前先校验按钮可用性，受限时带着抽屉上下文快速如实失败，不浪费一轮人工确认。
  - 新增错误码 `TASK_QUOTA_EXCEEDED`（可邀约额度不足或平台限制该操作）。
- **修复：抖店/微信切换时话术串台**。邀约表单的话术此前跨平台保留，切到另一平台的店铺时会把上一平台的话术带进新任务（实测把微信话术预填进了抖店任务）。现在跨平台切换自动清空话术并重置话术来源，同平台多店铺之间仍保留。
- **验证口径**：21 项单测全绿 + typecheck 全绿；临时实例（独立 userData，不影响正在运行的主实例与登录有效期探测）上复跑微信流程端到端：门禁批准→发送成功、门禁拒绝→取消、「停止邀约」→取消且面板复位，全部通过。抖店全流程待用户在真实店铺上重启应用后验证（涉及真实发送，仍以门禁为最终闸门）。

## 上一版（0.2.1）

- **产品模型落地：功能对应每个店铺**。逐项核对侧栏功能与店铺的归属关系：收藏（按店铺）、下载（按店铺）、环境指纹（按店铺）、截图（当前店铺的活动标签页）本就按店铺隔离；回收站（回收的是店铺本身）与设置（应用级）天然全局。**唯一不一致的是任务列表**——此前把所有店铺的任务混在一起、仅靠卡片上的店铺名区分。
  - **任务列表改为按当前店铺显示**：标题带店铺名，只列出当前店铺的任务；未打开店铺时只列历史遗留的"未绑定店铺"任务（此后新建任务一律绑定店铺）。
  - **新建任务绑定当前店铺**：从店铺面板打开「新建任务」时，绑定店铺固定为当前店铺（只读展示）；未打开店铺时才出现店铺下拉（并默认选中第一家，移除"不绑定"选项——未绑定任务在按店铺的列表里不可见，属于使用陷阱）。
  - 切换店铺时自动收起已展开的任务详情。任务引擎与数据层不变（任务本来就带 `store_scope`），纯界面归属修正。

## 上一版（0.2.0）

- **达人邀约新增微信小店支持（平台流程相互独立）**：设计约定每个平台的邀约功能各自独立，不做跨平台抽象猜配——抖店保持「批量勾选流」（batch-list），微信小店为全新「**辅助填单流**」（assist-form）。
  - **微信小店为什么必须逐个邀约**：实测（2026-09-13）其「带货者广场」每位达人一张独立表单，列表行没有勾选框；且达人详情页 URL 带每人专属 `finderUsername` token，**列表 DOM 里拿不到**——软件不猜测、不代选达人。选人由用户完成：打开达人广场 → 自己挑达人 → 进详情页点「邀请带货」→ 停在邀约表单页，回来点「开始邀约」。
  - **流程**：任务自建标签页**镜像**当前邀约页 URL（`mirrorTabUrl`）→ 受信任键鼠输入代填邀约联系人 / 微信号 / 手机号（至少一个）与合作说明（实测上限 **200 字**，抖音 150）→ 确保邀约商品（页面已有则原样不动；没有才走「添加商品」弹窗勾选，弹窗上限 30）→ **停下等确认** → 点「发送邀约」→ 等平台「确认发送邀约」弹窗出现 → 点「确认」→ **自动截图留档**。
  - **平台实测口径**：每日 **200 次**邀请额度（页面明示）；微信小店整页渲染在 `<micro-app shadowdom>` 的 **ShadowRoot** 里，普通 querySelector 不可见。
- **任务引擎新增 4 种步骤类型**：`mirrorTabUrl` / `typeText` / `waitForText` / `ensureRows`（白名单封闭、Zod `.strict()`、无任意代码入口；后三者进 `NON_RESUMABLE_TYPES`）。
  - **ShadowRoot 穿透（deep）**：`waitForSelector` / `readText` / `readTable` / `clickByText` / `aiGenerate` 支持 `deep: true`，按文档序深度优先枚举主文档与所有开放 ShadowRoot；微信流程的步骤全部 deep，抖店流程不带该参数、路径逐字未动。
  - **受信任输入（typeText）**：微信表单**不吃合成 input 事件**（原生 value setter + dispatchEvent 后框架状态不动，实测计数器/按钮无反应）——typeText 走浏览器输入管线：定位 → 滚动可见 → 真实点击聚焦（轮询 `getRootNode().activeElement` 确认，ShadowRoot 内 `document.activeElement` 只返回宿主、永远不相等，实测踩过）→ Ctrl+A → Delete → `insertText` → **回读校验**，不过如实报 `TASK_INPUT_NOT_APPLIED`（同内容重写幂等，内置一次整体重试吸收忙机竞态）。
  - **waitForText**：等「可见元素的自有文本」出现——微信的弹窗节点常**预渲染在 DOM 里（隐藏态也存在）**，`waitForSelector` 的存在性判断会被骗过，必须以可见文本为准。
  - **ensureRows**：自适应"确保页面可见行数 ≥ N"——已有则不动（避免对已有商品的邀约页重复添加），没有才点入口 → 弹窗勾选未选项 → 确认 → 复核。
  - **mirrorTabUrl**：在店铺其他标签页里找 URL 含 `urlIncludes` 的页面（人工打开的邀约页），把运行标签页导航过去并**切到前台**（门禁核对的就是这一页）；payload 只存 origin+path，**query 里的 finderUsername token 不落库**。找不到时如实报新错误码 `TASK_INVITE_PAGE_NOT_OPEN`。
  - **clickByText 新增 `mode:'real'`**：定位后发受信任鼠标点击——微信对框架托管的按钮/复选框，合成 click() 会点到隐藏克隆或被框架忽略（实测）。
- **修复：窗口遮挡/直启恢复时店铺视图 0×0**。渲染层从未上报视口尺寸时（窗口被遮挡期间切店铺，ResizeObserver 随渲染管线暂停、回调不触发），WebContentsView 一直保持 0×0——页面布局塌缩、点击全部落空。挂载时对近零视口退化为宿主内容区，渲染层一旦上报真实 bounds 即自动纠正。
- **修复两个存量类型错误**（0.1.5 起就带病存在）：`ai.models` 未登记进 `AuditAction` 联合类型；`ai-client` 模型列表推断为 `unknown[]`。`npm run typecheck` 恢复全绿。
- **步骤构造器下沉 shared**（`packages/shared/src/invite-steps.ts`）：面板与单测共用同一份序列构造，抖店/微信各自独立函数 + 按档案 `flow` 分派；新增单测 12 项（档案口径 / 两平台步骤序列 / schema 白名单拒绝非法输入），全套 21 项单测通过。
- **微信流程的验收方式（如实声明）**：任务引擎的四个新步骤 + 全流程（填表 → 商品 → 门禁 → 发送 → 确认弹窗 → 截图留档）与门禁拒绝路径（拒绝即取消、绝不发送）、负例（邀约页未打开时如实报错）均在**本地仿真页**（1:1 复刻微信表单/弹窗结构）上端到端跑通；**真实微信页面的selector锚点（placeholder/按钮文案/弹窗标题）来自 2026-09-13 实测**，但真实页面的完整跑通需登录态（会话过期需重新扫码），本次验收截至「填单+门禁拒绝在真实页面待复验」。门禁本身保证：未放行绝不发送。

## 上一版（0.1.5）

- **设置拆成四个独立页签：配置 / 达人广场 / AI 配置 / 关于软件**（每个页签各存各的，互不牵连）。
  - **达人广场**：按平台覆盖内置的达人邀约入口地址（留空 = 用已实测的默认地址，可"恢复默认"）。地址只做 `http(s)://` 格式校验、不联网探测；改成别的地址后，邀约任务的 `waitForPage` 就绪判据会**同步派生为新地址的末段路径**（不再写死 `daren-square`，否则自定义地址会一直等到超时）。
  - **AI 配置**：接口地址（OpenAI 兼容 `/chat/completions`）、模型名、API Key、超时；含「测试连接」与**「获取可用模型」**（只读 `GET /models`，地址由接口地址推导——推不出来就如实报 `AI_BAD_ENDPOINT`，绝不猜地址；拉回来的候选可在下拉里选，选中即填入模型名，改接口地址会立即作废旧候选）。
- **API Key 不进明文**：走系统 safeStorage（Windows DPAPI）加密存储（`credential-store` 的 `ai_cred.key`），IPC 只回 `hasKey: boolean`，**界面永不回显**（输入框始终留空、Dom 里既无明文也无 `enc:` 密文），并且 `AI_` 前缀设置键已在诊断包里排除——**导出诊断包不会带出 Key**。
- **AI 请求只在主进程**：渲染层 CSP 是 `connect-src 'self'`，本来也发不出去。如实声明的边界：AI 请求由主进程**直连出网、不走店铺代理**（Chromium 的 session 代理管不到主进程 fetch）；接口地址须 https://（仅本机 127.0.0.1 / localhost 允许 http://）；失败按 `AI_NOT_CONFIGURED / AI_BAD_ENDPOINT / AI_TIMEOUT / AI_REQUEST_FAILED / AI_EMPTY_OUTPUT` 如实报错，**不静默重试、不假装成功**。
- **达人邀约新增「话术来源」二选一：手填 / AI 生成（参考平台推荐商品）**。选 AI 时话术框只读（由运行时写入），未配置 AI 时「开始邀约」禁用并明确提示。AI 模式的步骤序列在手填版基础上，把"填话术"换成 **`aiGenerate`（读抽屉商品信息 → 主进程调大模型 → 写回话术框）+ `readText` 留档**，其余（勾权益 → **人工确认门禁** → 确认发送）不变。
  - **门禁仍然是最后一道闸**：AI 生成的话术在人放行前不会发出去，门禁文案明确写"由 AI 按平台推荐商品生成，请在页面话术框里核对后再放行"；拒绝即整单取消。
  - **可核查**：`aiGenerate` 的 payload 只存摘要（模型名 / 长度 / 来源字符数 / 前 40 字预览），**完整话术由紧随其后的 `readText` 步骤落库**——事后能查出"到底发了什么"。
- **`aiGenerate` 是第 5 种副作用步骤**：同样进 `NON_RESUMABLE_TYPES`（不可从失败恢复重试），参数仍是 Zod `.strict()` 白名单（`selector` / `sourceSelector` / `maxLen` / `instruction?`）、实现为固定注入脚本，**没有新增任何"执行任意代码"的入口**。
  - **商品来源的如实降级**：实测抖音精选联盟的邀约抽屉用哈希类名、结构随版本变，写死选择器等于猜测。因此平台档案的 `goodsSourceSelector` 留空 = 运行时从话术框向上找最近的**固定定位浮层**（即抽屉本体）读其可见文本；找不到浮层就报 `AI_EMPTY_OUTPUT` 失败，**绝不把整页噪音（达人列表、菜单）当商品信息喂给模型**。payload 里会记下这次到底用了哪条路径（`sourceHow`）。
- **修复：`readText` 读表单控件取的是当前 `value`**。此前一律读 `innerText || textContent`，而 `<textarea>` 的 `textContent` 是"默认值"，用 `setInput`/`aiGenerate` 写入后并不会变——照原样读会拿到空串，**留档就失真了**。现在 `input`/`textarea` 读 `value`，其它元素照旧。
- **修复：达人邀约任务此前根本创建不出来**（0.1.4 起就存在的真实缺陷）。步骤超时的 schema 上限被写死成 10 分钟，而邀约流程给「人工确认门禁」设的是 30 分钟、门禁本身的默认值更是 60 分钟——于是 `task:create` 一律以 `steps[N].timeoutMs` 超限被拒，界面只提示一句"步骤不合法"，用户点「开始邀约」永远没反应。上限已提到 1 小时（覆盖门禁语义），并补了三条边界断言：30 分钟门禁可创建 / 超 1 小时仍被拒 / 默认注入 60 分钟。
  - 这条是本次**新增的"点一下「开始邀约」并检查生成出来的任务步骤"断言**当场抓到的——此前 0.1.4 的验收只测了邀约面板的渲染与禁用态，没有真正走过"创建任务"这一步，所以漏了。教训：**面板类功能必须断言到"点下去之后真的发生了什么"**，只断言渲染等于没测。

## 上一版（0.1.4）

- **达人邀约（当前只做抖店）**：任务面板的「达人邀约」从占位变成可用功能——选主推类目、达人等级、本批数量、邀约话术、专属权益，一键生成受策展任务并执行：
  进入达人广场 → 选类目/等级 → 搜索 → **勾选前 N 位"未发过消息"的达人**（平台对已邀约过的行禁用复选框，执行时跳过并如实回报跳过数量）→ 打开邀约抽屉 → 填话术 → 勾权益 → **停下等你确认** → 才会点「确认发送」。
  - **安全边界**：提交前必有**人工确认门禁**，拒绝即整单取消、绝不继续；发送不可撤回且消耗平台邀约额度，所以门禁文案会把平台/类目/等级/数量/权益/话术全文一次摊开。
  - **只支持抖店**：其余平台的面板会明确显示"当前店铺的平台暂不支持"，不做猜测式实现。平台档案集中在 `packages/shared/src/constants/invite.ts`，后续按平台扩展只需追加一份档案。
  - **不保存敏感信息**：手机号/微信号与推荐商品由平台在抽屉里要求填写，本应用不落库、不代填手机号/微信号。
  - **平台实测口径**（2026-09-13）：单次勾选上限 40；话术 ≤150 字；推荐商品 ≤5 个；额度按「店铺类型 × 达人等级」下发（实测本店仅 LV0–LV3 有额度）。页面改版会让文案失配 → 任务如实报 `TASK_SELECTOR_CHANGED`，不静默重试、不假装成功。
- **任务引擎新增 4 种步骤类型**（`click` / `clickByText` / `clickAll` / `setInput`）：仍是白名单封闭——参数只有选择器与文本、Zod `.strict()` 校验、实现为固定注入脚本，**没有任何"执行任意代码"的入口**。四者均为副作用步骤，已进 `NON_RESUMABLE_TYPES`（不可从失败恢复重试，避免重复点击/重复提交）；写入沿用受控组件方式（原生 value setter + 派发 `input`/`change`），payload 只存长度摘要、不存原文。
  - 新增错误码 `TASK_TARGET_DISABLED`：点到禁用态目标时如实失败，而不是静默忽略。

## 上一版（0.1.3）

- **新增「设置」**（左栏底栏 `⚙ 设置`；左栏收起后的窄轨也可进入），含两个页签：
  - **配置**：为国内四家平台逐个设置「首页」地址（留空 = 用平台默认，可一键"恢复默认"）。地址栏左上角的 `⌂` 首页按钮按 **配置值 → 店铺自己的后台地址 → 平台目录默认** 取值——**配置过就以配置为准**（压过店铺自填地址）；新建店铺时的后台地址预填也改用同一来源。只做 `http(s)://` 格式校验，**不联网探测可达性**（与仓库"不猜测地址"的红线一致，填错首页就打不开，界面上如实提示）。
  - **关于软件**：软件名、版本、构建标签、仓库地址，以及**软件更新**——原先左上角的「↻ 更新」按钮已移除，更新整体搬到这里：stable/beta 双通道、启动时自动检查、检查更新 / 下载（SHA-512 校验）/ 重启并安装，状态与失败原因如实展示。
  - 顺带修掉一个老 bug：更新区"启动时自动检查"的复选框被 `.modal input{width:100%}` 撑成整行宽（实测 474px + 文字 = 558px），把弹窗顶出横向滚动条；现按 `.store-pick input` 的既有做法改为 `width:auto`，并补了"设置弹窗两个页签都无横向溢出"的断言。
- **任务面板新增二级页签：任务列表 / 达人邀约**。「达人邀约」在 0.1.3 为**占位**（界面如实标注"开发中，当前版本尚不可用"）——**0.1.4 已实现为可用功能，见上**。

## 上一版（0.1.2）

- **地址栏左上角新增「首页」按钮**（`⌂`，位于后退/前进/刷新之前的最左侧）：一键回到该店铺所在平台的**后台首页**。地址取该店铺的后台地址（新建时按所选平台自动填入、可自行修改），为空时回退到平台目录中该平台的默认后台地址——国内四家各自对应：拼多多 `mms.pinduoduo.com`、微信小店 `store.weixin.qq.com`、快手小店 `s.kwaixiaodian.com`、抖店 `fxg.jinritemai.com`；自定义平台（"其他"）用店铺自己填的后台地址。按钮悬停显示目标地址，未配置地址时置灰。

## 更早（0.1.1）

- **安全修复（随安装包发布）**：升级 `electron-updater` 6.6.2 → 6.8.9，其依赖的 `builder-util-runtime` 由 9.3.1 升到 **9.7.0**，修掉 CVE-2026-54673 —— 自动更新在遇到跨域重定向时会泄露 `PRIVATE-TOKEN` 与大小写变体的 `Authorization` 头。
  - 只升包不够：electron-builder 24 打包时按 `node_modules` 文件系统扁平化收集生产依赖，实测即使锁文件已全树解析为 9.7.0，它仍会从根目录那份遗留实体目录抄走旧的 9.2.4。已在 `pnpm-workspace.yaml` 用 `overrides` + `publicHoistPattern` 强制全树统一。
  - **以安装包为准复核**：解包 `app.asar` 实测 `electron-updater 6.8.9` + `builder-util-runtime 9.7.0`，单一版本无冲突。
- **左侧栏可收起/展开（用户要求）**：左栏收起为 **44px 窄轨**（保留 新建 / 回收站（带待办角标）/ 更新 / 展开 四个入口），展开态品牌行右侧新增 `‹` 收起按钮，`Ctrl+Shift+E` 随时切换，状态写入 `app_settings.ui.leftSidebarCollapsed` 并在启动时恢复。收起后中栏原生 `WebContentsView` 同步变宽（店铺页 `window.innerWidth` 实测 776→1036）。与右栏不同：左栏不设门禁（人工确认提示在右栏，收起左栏不会把它藏住）。
- **修复：回收站徽标计数不同步**：`trashStores` 原先只在点开回收站抽屉时才拉取，移入回收站后不刷新，导致「🗑 回收站」徽标（及左栏收起后的窄轨角标）漏显示；启动时也不加载，库里已有回收站店铺同样看不到。现 `init()` 与 `moveToTrash` 都刷新。
- **仓库清理**：移除 npm 时期遗留且内容过期的 `package-lock.json`（它连 electron-updater 都没记），并加入 `.gitignore`。
- **测试基建**：验收运行器与探针脚本统一改为结束整个进程树（原先只 kill 主进程，在 Windows 上会残留渲染进程僵尸）。

## 能力总览（M0–M4）

### 会话与 Cookie
- **会话导出/导入加密包**：导出经 scrypt（N=16384, r=8, p=1）+ AES-256-GCM 加密的 `.shopilot` 包，内含 Cookie 清单与店铺环境指纹配置；包携带有效期（默认 30 天），过期包一律拒绝导入。
- 口令由**主进程托管的独立窗口**采集（确认框 + 双次输入），不经过 Renderer，也不走业务 IPC 通道。
- 导入为覆盖语义：先清空目标店 Cookie 再写入；篡改包（GCM 认证失败）与结构异常包均拒绝导入，且**绝不动目标店现存会话**。
- **Cookie 查看器**：列表（值仅预览、HttpOnly/Secure 如实标记）、按名称/域搜索、单条删除、清空全部。
- 已知边界：导出包仅含 Cookie + 环境配置；**localStorage/IndexedDB 不在包内**；包为跨机传输设计（Cookie 为明文清单，安全性由容器口令加密保证）。

### 应用锁
- 主密码校验信息为 scrypt 派生值，整体再经系统 safeStorage（DPAPI）封装，**磁盘上不存在明文密码或明文校验值**。
- 锁定后：隐藏店铺浏览器视图、销毁代理认证等敏感引用、并**门禁全部业务 IPC**（仅解锁与状态查询可用）。
- 解锁需主密码；错误密码计次并写审计。空闲（5/15/30/60 分钟可选）自动锁定；`Ctrl+Shift+L` 可随时锁定。

### 代理健康巡检
- 绑定代理即刻体检、店铺浏览器打开时复检过期绑定、后台每 5 分钟巡检已绑定代理，状态变化通过 `proxy:healthChanged` 推送界面。
- 规范红线：**代理不可用时不自动切换、不静默降级**，只在界面如实展示失败原因，由用户决定。

### 备份与诊断
- **诊断包**（§22 脱敏）：版本、系统、数据库迁移版本与规模、代理体检摘要、白名单设置键、审计最近记录、脱敏日志；**不含** Cookie 值、代理凭据、会话原文、主密码信息。
- **审计日志导出**：JSONL 全量导出（可按条件过滤）。
- **运行/崩溃日志**：`userData/logs/app-YYYY-MM-DD.log` 按天落盘，单文件 5 MB 轮转（`.log.1`），**默认保留 14 天**并在启动时清理更早文件，敏感字样行整行掩码，renderer 错误与未捕获异常统一落盘。

### 发布工程（M4）
- electron-builder Windows x64 NSIS 安装包（可选安装目录、桌面与开始菜单快捷方式、卸载不删用户数据）。
- **自动更新（§21）**：electron-updater 接入 GitHub Releases（`Amike-cc/ShopPilot`），构建产出 `latest.yml`。设置 →「关于软件」页的更新区：检查更新 → 下载（完成后 SHA-512 校验，通过才提示可安装）→ 重启并安装；`stable`/`beta` 双通道（beta 含预发布版本，设置键 `update.channel`）；"启动时自动检查"可选（`update.autoCheck`，默认关闭，开启后启动 8 秒延迟检查一次）；检查/下载/安装与失败原因全部写审计（`update.check` / `update.download` / `update.install`）；feed 不可达时如实报错、绝不静默；环境变量 `SHOPPILOT_UPDATE_FEED` 可指向内部镜像（验收即用它驱动本地 feed 实测整条链路）。验收：`node update-runner.js`（16 项，含真实安装包 80MB 下载校验、pending 缓存落盘、双通道、故障路径、重启自动检查）。
- `release/release.json` 版本清单：产物 SHA-256、体积、Electron 版本、数据库 schema 版本、验收套件与已知边界。
- 安装/升级/卸载验收（13 项阶段检查全通过）：静默安装到自定义目录 → 运行冒烟 → 覆盖安装（数据保留 + 升级后目录完整性复核）→ 静默卸载（用户数据保留）。
- **单实例运行**：重复启动不再开第二个进程（避免两实例同写一个 SQLite 库），而是聚焦已有窗口并在日志留痕。
- 打包态**主进程托管对话**（会话导出确认框 + 口令框）纳入固定验收：在真实安装包内通过 CDP 点击按钮走完整链路（含"安全桥必须就绪"断言），不再是仅开发态可用。
- **环境面板布局修复**：新增的样式规则曾把"添加代理"行的协议下拉撑满、主机输入挤到 18px 并造成右栏横向溢出；现已收敛作用域，并把这四项布局要求写成固定断言（面板无横向溢出 / 协议下拉 74px / 主机输入 ≥60px / 三段分区存在）。
- **弹层遮挡修复**：店铺页面是原生 `WebContentsView`，永远绘制在 HTML 之上，导致回收站抽屉、新建店铺、任务对话框在店铺打开时被整块盖住（实测重叠 100%）。现在弹层打开会摘除视图挂载、关闭后自动恢复，锁定态与之互不干扰；断言以店铺页 `visibilityState` 从 `visible` → `hidden` → `visible` 为准。
- **店铺右键菜单（替换占位实现）**：此前右键店铺卡只会弹出系统确认框"确定 = 归档该店铺"。现为应用内菜单：打开/关闭浏览器、在独立窗口打开当前标签页、重命名、复制环境配置到其他店铺、复制店铺 ID、归档、移入回收站；支持 Esc 与点击外部关闭、贴边自动夹取，危险操作走应用内二次确认。菜单与店铺页面区域相交时会临时摘除视图，落在左栏则不受影响（中栏不会白闪）。
- **修复"复制环境配置"静默失败**：目标店铺若还没打开过浏览器（没有环境记录），旧实现会静默跳过，界面却提示成功。现在会先补建环境记录再复制，并如实回报"已复制 N 个，跳过 M 个（环境已锁定/店铺不存在）"。
- **右侧栏可以收起（用户要求）**：右栏收起为 44px 窄轨（图标=收藏/下载/环境/任务，点一下即展开并回到该面板；另有 `‹` 展开按钮），展开态页签右侧新增 `›` 收起按钮，`Ctrl+Shift+B` 随时切换，状态写入设置并下次启动自动恢复。收起后中栏原生视图同步变宽（店铺页 `window.innerWidth` 实测 760→1036px）。安全约束：**有待处理的人工确认时拒绝收起**并提示"处理完再收起"，收起状态下若来了新确认请求会自动临时展开，确保"需要人工确认"的提示永远不会被藏起来。
- **平台范围改为国内四家（拼多多 / 微信小店 / 快手小店 / 抖店）**：平台目录集中内置（主进程与界面同源），新建店铺按所选平台**自动填入后台地址**（拼多多 `mms.pinduoduo.com`、微信小店 `store.weixin.qq.com`、快手小店 `s.kwaixiaodian.com`、抖店 `fxg.jinritemai.com`，均可改；另有"其他"供自定义平台）。收藏面板新增**平台入口**（§16 平台适配层，只读展示、不重复入库）：微信小店与抖店提供后台首页/订单/商品/售后深链；拼多多与快手小店仅提供后台首页——因为这两家对任意路径都返回 200（统一跳登录页），未登录态无法验证深链真伪，宁缺毋滥以免给出 404 死链。入口段随附"页面改版可能调整地址"的提示。
- **任务功能修复（用户实报"任务功能不正确"）**：① 人工确认门禁此前只在"任务"面板里可见，切到收藏/下载/环境面板时任务会静默等待到超时——现在确认条常驻页签下方，任何面板都能看到并处理；② 新建任务对话框的步骤行被通用样式撑爆（超时/重试输入框 496px、整行 ~1100px 溢出，参数与删除按钮被挤出可视区），现已固定控件宽度并加"超时/重试"标签；③ 代理行、Cookie 行、任务卡与步骤行的删除按钮此前不可见且跑到窗口右上角（`.row-del` 定位规则写在基类上），现已可见并留在行内；④ 截图步骤在任务明细里不再显示为空，改为"截图工件 <文件名> · <哈希前 8 位>"。

## 安装

1. 运行 `ShopPilot-Setup-0.1.5.exe`，按向导安装（无需管理员权限，默认按用户安装）。
2. 首次启动自动初始化数据库与默认环境。
3. 卸载：控制面板或安装目录内 `Uninstall ShopPilot.exe`；用户数据默认保留（可用系统应用数据目录手动清理）。

## 已知边界（如实声明）

- 备份包跨机恢复覆盖元数据、配置与标签索引；**登录态**需在目标机重新登录，或经会话导出加密包导入（DPAPI 边界）。
- 任务引擎为**全局串行队列（并发=1）**；跨进程原地恢复不支持；人工确认节点默认 60 分钟超时。
- 截图步骤要求店铺浏览器处于可见视口，否则如实报 `CAPTURE_EMPTY` 失败。
- 安装包未做代码签名（无证书），Windows 可能提示"未知发布者"；自动更新仅做 SHA-512 哈希校验、无签名校验（INTERNAL_BUILD 边界）。
- **AI 请求由主进程直连出网、不走店铺代理**（Chromium 的 session 代理管不到主进程 fetch）；接口地址须 https://（仅本机 127.0.0.1 / localhost 允许 http://）。API Key 只在主进程使用，界面与诊断包都不会带出。
- **邀约抽屉里的"推荐商品"区域没有稳定选择器**（平台用哈希类名）：AI 模式默认读"话术框所在固定定位浮层"的可见文本；读不到就如实失败，不会拿整页文本充数。
- 自动更新内容托管于 GitHub Releases（**v0.1.5 已发布**：安装包 + blockmap + `latest.yml`，`node publish-release.js` 幂等上传；发新版本前先改 `package.json` 的 version 再跑 `pnpm run release:full`）；仓库已设为 **public**，打包态匿名在线检查实测通过（同版本如实返回"已是最新"）。启动自动检查默认关闭，可在设置 →「关于软件」开启。

## 回滚

- 保留上一版本安装包即可回退：卸载当前版本 → 安装旧版。数据库迁移为向前兼容（schema 版本仅升不降），回退到更早版本前请先备份 `userData`。
- 版本与产物校验：以 `release/release.json` 中的 SHA-256 为准核对安装包完整性。
