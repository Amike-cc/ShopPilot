# 仓库日志

这里存放仓库级构建、验收和诊断日志。日志文件默认被 Git 忽略，不进入源码提交。

- 根层：仓库级验收、构建和发布日志。
- `wx-invite-test/evidence/`：历史真机留档中的日志，保留原相对层级。
- 应用运行时日志不在这里，位置是 `%APPDATA%\shopilot\logs\`。
- `.npm-cache/_logs/` 属于包管理器缓存，不移动到这里。
