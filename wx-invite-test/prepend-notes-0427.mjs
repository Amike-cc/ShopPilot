/**
 * 把 0.4.27 的变更说明插到 RELEASE_NOTES.md 顶部（保留历史）。
 * 用文件而不是 node -e：中文经命令行传参在 Windows 上会被编码破坏（实测踩过）。
 */
const fs = await import('fs')
const NOTES = `# ShopPilot 0.4.27 变更说明（M0–M4）

> **构建标签：INTERNAL_BUILD** —— 本包未做代码签名，按 DEVELOPMENT_SPEC §21.5 不得标记为 RELEASE。可用于内部试用与验收，正式对外发布前需补签名证书与验收记录。

## 本次更新（0.4.27）：快手小店达人邀约（真实发送已跑通）

快手是「批量勾选流」的第二个平台，但交互与抖店差异很大，逐条都是真机量出来的。
**实机验证：彩排 29 步全通，随后真实发出 1 批（2 位达人），「我的达人 → 邀约中」出现
2026-09-15 14:38、达人反馈「待处理」的记录。**

### 快手邀约是什么样

入口在**另一个站点**：\`cps.kwaixiaodian.com\`（分销后台），不是商家后台。页面把筛选分成四行
（内容标签 / 带货类目 / 带货数据 / 合作信息），带货类目是**级联下拉**（18 项 + 各自子类），
「批量邀约」打开「带货邀约」抽屉：填联系人/手机号/微信号（都必填）→ 选商品 → 写带货描述 → 发送邀请。

### 为此新增的引擎能力（都按真机缺陷驱动）

1. **\`clickByText\` 支持"去空格后的文案"**：平台把按钮文案拆进子 span 并用 CSS 拉字距，
   \`innerText\` 是「确 认」「重 置」「查 询」，而元素的**自有文本是空串**——按自有文本精确匹配
   一个都命中不了。现在先按自有文本找（既有平台零变化），找不到再按**规范化 innerText** 兜底。
2. **\`clickAll.skipSelector\`**：表头的"全选"复选框与商品行同选择器时，把它排除掉
   （不排除会被当成一个候选，点它=全选，不是"勾 N 个"）。
3. **\`clickAll.counterIncludes\`**：同一页面上有多个「已选N…」时限定用哪个计数。
   实测踩到：在商品弹窗里勾选时，页面上达人选人区的「已选2条」还在，被当成本次计数 →
   \`progress()\` 恒为 0 → 误报"只勾中 0 位"（其实商品勾上了）。不给它时只认抖店那种
   明确写成「已选择N位达人」的计数。
4. **\`clickIfPresent.nearText\`**：只在"祖先链含该锚点文案"的范围里找目标按钮。
   实测踩到：商品弹窗的「确 认」与它上面弹出的警告框「确认」**同名**，按文案找会命中下层那个，
   点了等于白点、警告框一直留着；而两者共享外层容器，所以"先找锚点再往上取浮层"也失效——
   判据必须是"候选按钮的祖先链里包含锚点"。
5. **\`requireQuota.code\`**：这枚通用"数字 ≥ min"断言可换错误码了。
   断言**额度**时用默认码（loop 的 \`stopOn\` 里 = 正常收尾）；断言**别的**（如"已选商品数 ≥ 1"）
   必须换一个不在 \`stopOn\` 里的码，否则会把"商品没选上"当成"按预期收工"——
   一位都没邀约却报成功，这是最危险的一类静默失败。新增 \`TASK_PRODUCT_NOT_SELECTED\`。
6. **\`pressKey\`（白名单只有 Escape）**：收起页面上残留的下拉浮层。
   实测：选完带货类目后级联下拉一直展开着，盖住后面要点的按钮。

### 快手档案（\`packages/shared/src/constants/invite.ts\`）

- \`pageUrl\` 用分销后台的达人广场地址；\`minSelect: 2\`（**实测只勾 1 位点「批量邀约」没有任何反应**，
  勾 2 位才打开抽屉）——步骤构造会把本批要勾的人数抬到这个下限。
- 没有「达人等级」筛选（LV0 是达人自身属性）→ \`levels\` 为空，面板与步骤都跳过。
- 四行筛选只登记需要用户选的：内容标签（37 项）、合作信息（4 项）；带货类目 18 项 + 实测子类。
- \`quotaCheck: 'requireQuota'\`：抽屉底部**明示**「今日剩余100条发送邀请机会」（抖店只能看按钮禁用）。
- 商品：点「选择商品」开弹窗 → 勾商品 → 点「确 认」。**商品是必选的**（实测点「发送邀请」会提示
  「请选择商品」）。弹窗里的确认后，平台**可能**再弹一个警告框（文案是动态的：
  「商品不符合达人带货要求…」/「商品佣金率低于达人带货要求…」，共有句是「确认是否仍要发送邀请」），
  用 \`clickIfPresent\` 出现就点掉。之后用「已选择商品数 ≥ 1」做结果校验。

### 面板

批量流程的文案改为按档案渲染（抖店文案不变）：类目标题「带货类目」、标签区「合作标签」、
多出「内容标签 / 合作信息」两行可多选、以及三个必填联系方式输入框；
数量输入的下限跟随 \`minSelect\` 并给出说明。

### 已知边界

- 「邀约中」列表实测有 5330 条历史记录（多为平台侧的存量），本功能只负责"发出新的邀约"。
- 快手没有等级筛选，抖店那套「额度按店铺类型×等级下发」的说明不适用。
- 商品弹窗里的商品来自店铺的推广计划；列表为空时该步骤会如实失败（不是静默跳过）。

---

`
const old = fs.readFileSync('docs/RELEASE_NOTES.md', 'utf8')
if (old.startsWith('# ShopPilot 0.4.27')) {
  console.log('已经是 0.4.27，跳过')
} else {
  fs.writeFileSync('docs/RELEASE_NOTES.md', NOTES + old, 'utf8')
  console.log('已插入 0.4.27 说明；文件长度', NOTES.length + old.length)
}
const check = fs.readFileSync('docs/RELEASE_NOTES.md', 'utf8')
console.log('含 0.4.26 历史:', check.includes('本次更新（0.4.26）'))
