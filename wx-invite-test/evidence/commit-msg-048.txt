feat(invite): v0.4.8 主推类目支持二级类目 + 邀约面板 UI 重构

1) 二级类目：
   - 面板：一级/二级联动下拉（二级默认"不限"，切一级自动回"不限"并换选项）；
   - 数据：categoryTree 入档（22 个一级的子类全部来自平台级联逐个实测 2026-09-14，
     弹层懒创建——一页只点一个 chip 才能读到对的子类；个别长名平台侧有截断，按包含匹配）；
   - 执行：点一级 chip 后点所选二级项，并在「已筛选」连带校验二级名；
   - 真机抓到的平台交互：带下级的二级项吃合成点击只展开不选中 → 改受信任真实鼠标点击；
   - 配置保存扩展 subcategory（联动校验后恢复）。

2) 面板 UI 重构：三张分区卡片（①选人范围/联系方式 ②邀约内容 ③运行），抖店/微信同构，
   原 data-test 钩子与保存行为不变。

3) 真机验证：
   - 不发送（家清纸品）：筛选生效（已筛选含 个护家清+家清纸品），池子仅 8 位 →
     发送前如实失败（TASK_SELECTION_SHORTFALL，报明 8/40、9 位已邀约）；
   - 真实发送（食品饮料/休闲食品，LV0-3，40 位）：已筛选=主推类目：食品饮料/休闲食品，
     页面自认已选 40，平台提示「共发送 40 位：33 成功 / 7 失败」，随后停止邀约取消成功；
   - 7 位失败为平台侧逐人结果（页面仅汇总），软件以抽屉关闭=受理为判据，已在说明中注明。

typecheck + 单测 34 通过。
