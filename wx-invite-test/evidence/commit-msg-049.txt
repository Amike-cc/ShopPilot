feat(invite): v0.4.9 微信小店邀约——广场筛选（类型/类目/其他）+ 按商品ID指定邀约商品

1) 面板新增「① 广场筛选」卡片：带货者类型（4 项单选，实测清单）、带货类目（34 项多选）、
   其他筛选（可开发票/有联系方式）；条件持久化到 invite.config.微信小店。
2) 「打开达人广场」按配置自动应用筛选（新 IPC browser:prepareInviteSquare）：
   微信页在 micro-app ShadowRoot 内 → 深度枚举定位坐标 → 主进程发受信任鼠标事件
   （实测合成 click 在微应用里不生效，勾选状态不变）。
3) 邀约商品按 ID 指定：面板「指定邀约商品 ID」（多 ID 分隔）+ 新引擎步骤 ensureRowsById
   （打开添加商品弹窗 → 按行文本包含 ID 匹配 → 逐行勾选 → 确认 → 复核；匹配不到如实失败，
   提示确认商品ID正确且已上架）。
4) 门禁消息写明指定的商品 ID。

真机验证（不依赖登录，用带 ShadowRoot 的仿真页跑真实应用+真实引擎）：
- 筛选应用：类型=直播带货者、类目=母婴/美妆护肤、其他=有联系方式全部生效
  （页面标签「已筛选 类型：直播带货者 带货类目：母婴 带货类目：美妆护肤 其他筛选：有联系方式」）；
- 商品ID指定：10000687986402 → 只勾中该行 → 确认后页面出现该商品，payload {requested:1,added:1,present:1}；
- 面板四卡片/34 类目/2 其他筛选/商品ID 输入框在真实界面确认，改动自动落库。
待复验：微信小店登录态已过期（登录超时、Cookie 空），二维码已打开待用户扫码；
扫码后在真实广场/表单补跑最终验证。
单测 35 通过（含微信筛选档案、ensureRowsById 分支与 schema）。
