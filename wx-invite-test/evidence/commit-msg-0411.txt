feat(invite): v0.4.11 微信小店邀约真实发送跑通 + 四个真问题修复

按用户要求在内置店铺浏览器跑真实流程，真实发出 1 条达人邀约（run succeeded，平台侧表单被清空）。

修复：
1) 微信号与手机号都必填（用户指出）：只填微信号时点发送邀约无任何反应（平台弹窗不出现），补手机号后弹窗正常、确认可点、发送成功。面板两项都设为必填并写明原因。
2) 窄视口下页面布局重叠导致筛选项被压住点不到（真实页面 776px 下 美妆护肤 被 DT 压住）：定位改为在元素矩形内取 15 个采样点、用 elementFromPoint（穿 ShadowRoot）挑未被遮挡的点；全被遮挡则如实报告遮挡者。
3) 类型页签选中态在父级 LI 的 nav_current：校验改为向上查 3 层。
4) 校验脚本里混入 TS 类型标注（let node: Element | null = el）在页面里是语法错误，导致筛选校验恒失败、全部误报未选中。

实测（内置浏览器）：筛选 类型/母婴/有联系方式 生效（美妆护肤如实报遮挡）；详情→邀请带货→表单；代填联系人+微信号+手机号+话术；ensureRowsById {requested:1,added:1,present:1}；额度预检通过 → 门禁放行 → 确认发送邀约弹窗 → 确认 → succeeded → 表单被平台清空。

typecheck + 单测 35 通过。
