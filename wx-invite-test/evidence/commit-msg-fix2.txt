fix(invite): 修掉校验脚本里的 TS 类型标注（页面端语法错误导致筛选校验恒失败）

在仿真页回归时发现：stateOf 校验脚本里写了 `let node: Element | null = el`——这段字符串是发给
浏览器执行的，类型标注在页面里是语法错误，导致校验函数每次都抛错、被 catch 成"未选中"，
于是所有筛选项都误报"点击后未确认选中"（实际点击是生效的）。去掉标注后仿真回归两项调用
applied 均为 true、四项逐项 ok。

typecheck + 单测 35 通过。
