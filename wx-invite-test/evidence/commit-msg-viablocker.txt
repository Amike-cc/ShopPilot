fix(invite): 遮挡者若是同行内的可点链接则点它（微信「详情」被整卡链接盖住）

真机实测：微信广场每个达人行里「详情」被同行 <a class="w-full text-right"> 覆盖
（平台把整卡做成可点链接）——上一版把它判成 COVERED 直接失败。
现在：全采样点被遮挡时，若遮挡者（或其祖先）是可点元素且与目标在**同一行/同一卡片**内，
就点它（用户实际会做的事）；跨行绝不乱点，仍如实报 COVERED。
仿真页验证：点「详情」最终落到同行覆盖链接（页面显示「覆盖链接被点击: row-1」），run succeeded。
单测 37 项通过。
