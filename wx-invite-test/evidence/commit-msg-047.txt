feat(invite): v0.4.7 邀约配置可保存 + 真实运行验证中修掉三个引擎缺陷

1) 配置按平台持久化（settings 表，防抖 500ms 回写、启动时合并恢复；抖店/微信各存各的）。
   真机测试抓出两个真 bug 并修：
   - IPC 结构化克隆不认 Vue 响应式 Proxy：levels/benefits 快照导致 settings.set 一直抛
     "An object could not be cloned" 且被 void 吞掉 → 发送前 JSON 转纯对象；
   - settings.get 返回 {key,value} 包装，读取时忘拆 → 永远读不到存档 → 取 .value。
   真机验证：写入特征配置 → 存档一致 → 重启后面板逐字段恢复。

2) 真实运行验证（面板点开始邀约、真实店铺）抓出两个引擎缺陷并修：
   - loop 嵌套步骤没有补默认 timeoutMs（只有顶层补）→ withTimeout(NaN)
     真机报「clickByText 超过 NaNms」→ 创建时归一化 + 运行时兜底；
   - 第一版递归校验把子步骤存成"只有 input 没有 type"→ 运行报「未知步骤类型 undefined」
     → 重构为返回完整步骤 {type,input,timeoutMs,retryLimit?}。
   另更新面板启动 toast 文案（抖店已无二次确认）。

3) 真实运行结果（如实汇报）：筛选铁证=运行标签页「已筛选：主推类目：个护家清/不限
   达人等级：LV0，LV1，LV2，LV3」，流程自带类目校验连续 12 轮通过；循环邀约每轮约 20s
   完整走完（筛选→逐个勾40→批量邀约带货→额度预检→话术→确认发送→抽屉关闭校验→截图）；
   测试监听盯错标签页导致停止偏晚，实际发出约 11 批（约 440 条真实邀约）后点停止成功。
   「额度用完即停」的收尾语义已在仿真站点验证；真实店铺上会在平台禁用确认发送时自动停。

typecheck + 单测 33 通过。
