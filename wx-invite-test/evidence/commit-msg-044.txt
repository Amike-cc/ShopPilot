feat(ui): v0.4.4 任务面板新增二级页签 +「待开发」占位页签

- 任务面板顶部加页签栏：达人邀约（默认，现状不变）/ 待开发（占位，标签带黄点）。
- 占位页写明"尚未开发"，并交代位置与达人邀约同级、沿用同一套约束（任务引擎白名单步骤、
  提交类动作前置人工确认门禁、结果与截图落库、失败如实回报）。
- 用 v-show 切换而非 v-if 销毁重建：来回切页签时正在填的邀约配置不丢。
- 二级页签不复用 .panel-tabs（那行是标题栏拖拽区、给窗口按钮留了 140px），用独立 .sub-tabs，
  避免内容区页签抢拖拽并凭空留白。
- 以后加任务类功能：TASK_SUB_TABS 加一条 + 落一个内容面板即可，面板布局不用再动。

实测（真机重启后）：页签栏渲染正常；默认=达人邀约（invite 显示 / todo 隐藏）；
点「待开发」显示互换、高亮跟随；切回后「开始邀约」与已填配置仍在；typecheck + 单测 28 通过。
本次只加界面结构与占位，不新增平台能力、不影响邀约流程。
